% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/afl_api.R
\name{get_afl_player_season_stats}
\alias{get_afl_player_season_stats}
\title{Fetch AFL statspro season-total player stats (extended fields)}
\usage{
get_afl_player_season_stats(season = NULL, comp = "AFLW")
}
\arguments{
\item{season}{Numeric year (default: current season via \code{\link[=get_afl_season]{get_afl_season()}})}

\item{comp}{Competition: "AFLM" or "AFLW" (default). Built AFLW-first
because that's where the gap is (see field-audit note above), but the
endpoint and field set work identically for AFLM -- no comp-specific
branching needed.}
}
\value{
A tibble, one row per player who played this comp-season, with
\code{player_id}, \code{season}, \code{comp}, \code{team_abbr}, \code{games_played}, and the 63
snake_cased \verb{totals.*}/\verb{averages.*} fields (averages suffixed \verb{_avg}).
}
\description{
Fetches the \code{statspro/playersStats/seasons/{providerId}} endpoint — one
HTTP call returns every player's season totals/averages for a comp-season,
including 28 "extended" fields that the CFS \code{playerStats/match/{matchId}}
\code{extendedStats} block returns genuinely empty for AFLW (confirmed
2026-08-25; see \code{AFL-API-REFERENCE.md}'s "Endpoint family: statspro"
section for the full field-by-field audit against \code{\link[=get_afl_player_stats]{get_afl_player_stats()}}).
Season-total granularity only -- the round-level sibling endpoint
(\code{playerSeasonRoundStats}) does NOT carry these 28 fields at all (confirmed
absent, not just null), and the season endpoint has no as-at-round/cutoff
parameter -- the client never sends one, and \verb{&roundId=...} was tested live
and had no effect. So this is genuinely the finest grain available for
these specific fields.
}
\details{
Same anonymous \code{x-media-mis-token} auth as \code{\link[=get_afl_player_stats]{get_afl_player_stats()}} --
reuses \code{\link[=get_token]{get_token()}}'s 5-minute cache.
}
\examples{
\dontrun{
season_stats <- get_afl_player_season_stats(2025, comp = "AFLW")
}

}
