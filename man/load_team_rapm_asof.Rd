% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rapm_match_feature.R
\name{load_team_rapm_asof}
\alias{load_team_rapm_asof}
\title{Load the as-of xRAPM snapshot table for a competition}
\usage{
load_team_rapm_asof(comp = "AFLM", path = NULL)
}
\arguments{
\item{comp}{"AFLM" (default) or "AFLW".}

\item{path}{Optional explicit path, primarily for tests.}
}
\value{
A data.frame with at least \code{player_id}, \code{season}, \code{round_number},
\code{team_rapm_shrunk}; or \code{NULL} if no snapshot is available.
}
\description{
Load the as-of xRAPM snapshot table for a competition
}
\section{Infrastructure gap}{

Unlike every other rating input to the match model, this snapshot is \strong{not}
published to a torpdata release and has no entry in the \verb{load_*()} family
proper. It is a local build artifact written by
\code{data-raw/03-ratings/build_team_rapm_asof_snapshots.R}, and that path is
gitignored. Consequences, stated plainly rather than discovered later:
\itemize{
\item A clean checkout (and therefore CI, and therefore the scheduled prediction
workflow) has no snapshot file, so \code{xrapm_diff} falls back to neutral 0 --
see \code{.build_team_ratings_df()}'s caller-side handling.
\item The snapshot must be REBUILT as the season advances. It is a per-round
artifact; a stale file silently stops gaining checkpoints, and the rolling
join then reuses the last available round's ratings for every later match.
That degrades quietly rather than erroring.
}

Making this feature genuinely live requires publishing the snapshot to a
torpdata release and refreshing it on the same cadence as the ratings
pipeline. That work does not exist yet.
}

\keyword{internal}
