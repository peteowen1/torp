% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_v3.R
\name{compute_aerial_credit}
\alias{compute_aerial_credit}
\title{Per-player aerial contest credit}
\usage{
compute_aerial_credit(
  chains,
  pbp_data,
  leak_safe = TRUE,
  alloc = EPV_CONT_LOSS_ALLOC,
  exposure_descs = EPV3_AERIAL_EXPOSURE_DESCS,
  player_stats = NULL
)
}
\arguments{
\item{chains}{Raw chains data.}

\item{pbp_data}{Clean PBP.}

\item{leak_safe}{Fit per season on earlier seasons only.}

\item{alloc}{Loser-allocation rule: \code{"team"} (default and the only one
that survived measurement), \code{"none"}, \code{"prorata"} or
\code{"ledger"}. See \code{EPV_CONT_LOSS_ALLOC}.}

\item{exposure_descs}{Chain descriptions counting as exposure, used only by
\code{"prorata"}.}

\item{player_stats}{Required by \code{"ledger"}; ignored otherwise.}
}
\value{
A data.table of \code{player_id}, \code{match_id},
\code{epv_cont_aerial}, \code{epv_disp_aerial}, \code{contests_won},
\code{contests_lost}.
}
\description{
Fits the branch models on a strictly EARLIER window than the season being
scored when \code{leak_safe} is TRUE, which it must be for anything feeding
the match-model gate: the ratings are features of that model, so branch models
fitted on the full history would leak the future into an OOS evaluation.
}
\keyword{internal}
