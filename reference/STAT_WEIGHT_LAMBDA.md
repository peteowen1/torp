# L2 (ridge) regularization lambda for count-based stat weights

L2 (ridge) regularization lambda for count-based stat weights

## Usage

``` r
STAT_WEIGHT_LAMBDA
```

## Format

An object of class `numeric` of length 1.
