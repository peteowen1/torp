# Default log-distance value when venue coordinates are missing

Default log-distance value when venue coordinates are missing

## Usage

``` r
MATCH_LOG_DIST_DEFAULT
```

## Format

An object of class `numeric` of length 1.
