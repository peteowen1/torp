# Grand Final venue (standardised name via torp_replace_venues)

Grand Final venue (standardised name via torp_replace_venues)

## Usage

``` r
SIM_GF_VENUE
```

## Format

An object of class `character` of length 1.
