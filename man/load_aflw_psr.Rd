% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_aflw_psr}
\alias{load_aflw_psr}
\title{Load AFLW Player Skill Ratings (PSR)}
\usage{
load_aflw_psr(
  seasons = get_afl_season(),
  use_disk_cache = FALSE,
  columns = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years, or \code{TRUE} for every
available AFLW season (2018 onwards).}

\item{use_disk_cache}{Logical. If \code{TRUE}, uses persistent disk cache for
faster repeated loads. Default is \code{FALSE}.}

\item{columns}{Optional character vector of column names to read. If NULL
(default), reads all columns.}
}
\value{
A data frame of AFLW PSR with columns including \code{player_id},
\code{player_name}, \code{season}, \code{round}, \code{pos_group},
\code{psr_raw}, and \code{psr}.
}
\description{
Loads pre-computed AFLW PSR from the
\href{https://github.com/peteowen1/torpdata}{torpdata repository}, release tag
\code{aflw_psr-data}. The AFLW equivalent of \code{\link[=load_psr]{load_psr()}}, kept as its own
release rather than a comp column on \code{psr-data} so the men's published
artifact -- which live consumers read -- is never rewritten by an AFLW run.
}
\details{
Deliberately does NOT call \code{validate_seasons()}. That helper floors
at \code{AFL_MIN_SEASON} (2021) because that is where torp's men's chain data
starts, but AFLW's box-score history runs from 2018 -- passing it through
would abort on exactly the three earliest seasons this release exists to
carry.
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_aflw_psr(2025)
  load_aflw_psr(TRUE)  # all AFLW seasons
})
}
}
\seealso{
\code{\link[=load_psr]{load_psr()}}, \code{\link[=calculate_psr]{calculate_psr()}}
}
