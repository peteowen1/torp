# Descriptions for ground ball contests (present in both chains and PBP) Adjacent ground ball rows from opposing teams at same x,y indicate a contest

Descriptions for ground ball contests (present in both chains and PBP)
Adjacent ground ball rows from opposing teams at same x,y indicate a
contest

## Usage

``` r
CONTEST_GROUND_BALL_DESCS
```

## Format

An object of class `character` of length 5.
