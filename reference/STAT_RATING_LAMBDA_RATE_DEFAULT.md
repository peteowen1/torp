# Default exponential decay rate for rate stats (per day) Half-life = ln(2) / 0.0019 ~ 365 days

Default exponential decay rate for rate stats (per day) Half-life =
ln(2) / 0.0019 ~ 365 days

## Usage

``` r
STAT_RATING_LAMBDA_RATE_DEFAULT

SKILL_LAMBDA_RATE_DEFAULT
```

## Format

An object of class `numeric` of length 1.

An object of class `numeric` of length 1.
