% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_fixtures}
\alias{load_fixtures}
\title{Load AFL Fixture Data}
\usage{
load_fixtures(
  seasons = NULL,
  all = FALSE,
  use_disk_cache = FALSE,
  use_mem_cache = TRUE,
  cache_ttl = 3600,
  verbose = FALSE,
  columns = NULL,
  use_cache = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years associated with given AFL seasons - defaults to latest season. If set to \code{TRUE}, returns all available data since 2021.}

\item{all}{Deprecated. Use \code{seasons = TRUE} instead (consistent with other \verb{load_*()} functions).}

\item{use_disk_cache}{Logical. If TRUE, uses persistent disk caching. Default is FALSE.}

\item{use_mem_cache}{Logical. If TRUE, uses in-memory cached data when available to speed up repeated calls. Default is TRUE.}

\item{cache_ttl}{Numeric. Time-to-live for cached data in seconds. Default is 3600 (1 hour).}

\item{verbose}{Logical. If TRUE, prints cache hit/miss information.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}

\item{use_cache}{Deprecated. Use \code{use_mem_cache} instead.}
}
\value{
A data frame containing AFL fixture and schedule data.
}
\description{
Loads AFL fixture and schedule data from the \href{https://github.com/peteowen1/torpdata}{torpdata repository}
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_fixtures(2021:2022)

  # Load all fixtures
  load_fixtures(seasons = TRUE)
})
}
}
\seealso{
\code{\link[=load_results]{load_results()}}, \code{\link[=load_teams]{load_teams()}}, \code{\link[=load_predictions]{load_predictions()}}
}
