% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versebus.R
\name{.vb_asset_true_size}
\alias{.vb_asset_true_size}
\title{Read an asset's true stored size, bypassing the release listing}
\usage{
.vb_asset_true_size(repo, tag, file_name)
}
\arguments{
\item{repo}{"owner/name".}

\item{tag}{Release tag.}

\item{file_name}{Asset file name, including extension.}
}
\value{
Size in bytes, or \code{NA_real_} if it could not be determined.
}
\description{
The release-listing endpoint can serve the PREVIOUS asset's row for a few
seconds after an upload (torpdata#74). When that happens there is no way to
tell a lagging read from a lost write out of the listing alone, because every
field in it — size, \code{updated_at}, even the asset \code{id} — belongs to the stale
row. Asking by \code{id} therefore just re-confirms the stale answer.
}
\details{
The release DOWNLOAD path resolves by \strong{name}, so it cannot hand back the
previous asset. A one-byte ranged GET against it costs a single byte on the
wire and reports the object's real length in \code{Content-Range}.

Best-effort by contract: \strong{every} failure path returns \code{NA_real_}, including
a private repo whose download redirect needs credentials we do not have. This
must never become the reason an upload that succeeded is reported as failed —
callers fall back to whatever they did before.
}
\keyword{internal}
