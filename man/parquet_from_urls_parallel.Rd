% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_engines.R
\name{parquet_from_urls_parallel}
\alias{parquet_from_urls_parallel}
\title{Download multiple parquet files in parallel using curl}
\usage{
parquet_from_urls_parallel(
  urls,
  use_cache = FALSE,
  max_age_days = 7,
  columns = NULL
)
}
\arguments{
\item{urls}{Character vector of URLs}

\item{use_cache}{Logical. If TRUE, also check/write the \verb{~/.torp/cache/} disk cache.}

\item{max_age_days}{Maximum disk cache age in days.}

\item{columns}{Optional character vector of column names to select.}
}
\value{
A data.table with all files combined
}
\description{
Always checks local \verb{torpdata/data/} first for each URL (with smart
staleness), then optionally checks disk cache, then downloads missing
files in parallel. Local files are batch-read via \code{arrow::open_dataset()}
for speed. Downloaded data is auto-saved to local storage.
}
\keyword{internal}
