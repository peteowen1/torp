% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/local_data.R
\name{download_torp_data}
\alias{download_torp_data}
\title{Download TORP Data for Local Storage}
\usage{
download_torp_data(data_types = "all", seasons = TRUE, overwrite = FALSE)
}
\arguments{
\item{data_types}{Character vector of data types to download. Options:
\code{"pbp"}, \code{"chains"}, \code{"ep_wp_chart"}, \code{"fixtures"}, \code{"player_stats"},
\code{"player_details"}, \code{"player_game"}, \code{"results"}, \code{"xg"}, \code{"teams"},
\code{"predictions"}, \code{"player_game_ratings"}, \code{"player_season_ratings"},
\code{"torp_ratings"}, \code{"team_ratings"}.
Default is \code{"all"} which downloads all types.}

\item{seasons}{A numeric vector of 4-digit years, or \code{TRUE} for all
available seasons (2021 to current). Default is \code{TRUE}.}

\item{overwrite}{Logical. If \code{FALSE} (default), skips files that already
exist locally.}
}
\value{
Invisible NULL. Called for side effects (file downloads).
}
\description{
Downloads parquet files from GitHub releases to the local \verb{torpdata/data/}
directory for fast offline access. Subsequent calls to \code{load_pbp()},
\code{load_chains()}, etc. will read from local files instead of downloading.
}
\examples{
\dontrun{
# Download all PBP data
download_torp_data("pbp")

# Download multiple types for specific seasons
download_torp_data(c("pbp", "chains"), seasons = 2024:2025)

# Download everything
download_torp_data()
}
}
