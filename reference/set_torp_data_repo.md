# Set TORP Data Repository

Sets the repository used for TORP data downloads in the current session.

## Usage

``` r
set_torp_data_repo(repo)
```

## Arguments

- repo:

  Character string of the repository in format "owner/repo"
