% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_player_skills}
\alias{load_player_skills}
\title{Load Player Skills Data}
\usage{
load_player_skills(
  seasons = get_afl_season(),
  use_disk_cache = FALSE,
  columns = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years associated with given AFL
seasons — defaults to latest season. If set to \code{TRUE}, returns all
available data since 2021.}

\item{use_disk_cache}{Logical. If \code{TRUE}, uses persistent disk cache for
faster repeated loads. Default is \code{FALSE}.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}
}
\value{
A data frame containing player skill estimates with columns
including \code{player_id}, \code{player_name}, \code{pos_group}, \code{n_games},
\code{wt_games}, \code{ref_date}, and \verb{\{stat\}_skill}, \verb{\{stat\}_lower},
\verb{\{stat\}_upper} for each estimated stat.
}
\description{
Loads pre-computed Bayesian player skill estimates from the
\href{https://github.com/peteowen1/torpdata}{torpdata repository}.
Skills are per-stat estimates with credible intervals, produced by
\code{estimate_player_skills()}.
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_player_skills(2024)
})
}
}
\seealso{
\code{\link[=estimate_player_skills]{estimate_player_skills()}}, \code{\link[=player_skill_profile]{player_skill_profile()}}, \code{\link[=load_player_game_ratings]{load_player_game_ratings()}}
}
