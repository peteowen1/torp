% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_train.R
\name{.oof_predict_gam}
\alias{.oof_predict_gam}
\title{Out-of-fold prediction for one stacked-cascade GAM stage}
\usage{
.oof_predict_gam(formula, data, weights, family, gamma_arg, nthreads, folds)
}
\arguments{
\item{formula}{Model formula, identical to the one used for the full fit.}

\item{data}{Training data (one cascade stage's \code{gam_df}).}

\item{weights}{Numeric weights vector, same length as \code{data}.}

\item{family}{GAM family (e.g. \code{gaussian()}).}

\item{gamma_arg}{Smoothness penalty multiplier, matching the full fit.}

\item{nthreads}{Threads for \code{mgcv::bam()}.}

\item{folds}{List of integer row-index vectors, one per held-out fold.}
}
\value{
Numeric vector, \code{length(data)} rows, out-of-fold predictions.
}
\description{
Refits \code{formula} once per season fold, holding that season's rows out of
training and predicting only onto them, then assembles the held-out
predictions into one vector aligned to \code{data}'s row order. Used so a
cascade stage's own in-sample fit is never fed forward as the next
stage's input feature (see the header comment on \code{.train_match_gams()}
and \code{.train_match_xgb()} for why this matters).
}
\keyword{internal}
