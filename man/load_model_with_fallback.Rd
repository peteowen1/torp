% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_variables.R
\name{load_model_with_fallback}
\alias{load_model_with_fallback}
\title{Load Model from torpmodels}
\usage{
load_model_with_fallback(model_name)
}
\arguments{
\item{model_name}{Short model name: "ep", "wp", "wp_calibration", "shot",
"match_gams", or "xgb_win"}
}
\value{
The loaded model object, or \code{NULL} for \code{"wp_calibration"} when
unavailable.
}
\description{
Loads a model from the torpmodels package with in-memory caching.
Install torpmodels via \code{devtools::install_github("peteowen1/torpmodels")}.
}
\details{
\code{"wp_calibration"} is the one exception to the usual abort-on-failure
contract: it's an optional sidecar (torpverse/docs/plans/FABLE-RECAL-PLAN.md D3/D4), so a
404/absence/network failure degrades to a single \code{cli_warn()} and a
cached \code{NULL} -- never an error, and never more than one warning per
session (the \code{NULL} is cached like any other model, so subsequent calls
return it from cache before reaching the network/warning code at all).
Every other model name keeps the hard-abort contract.
}
\keyword{internal}
