# Decay rate (per day) for opponent defensive profile recency weighting. ~231-day half-life, slightly faster than stat rating decay.

Decay rate (per day) for opponent defensive profile recency weighting.
~231-day half-life, slightly faster than stat rating decay.

## Usage

``` r
OPP_ADJ_LAMBDA_DECAY
```

## Format

An object of class `numeric` of length 1.
