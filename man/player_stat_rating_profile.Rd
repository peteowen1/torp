% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_skills.R
\name{player_stat_rating_profile}
\alias{player_stat_rating_profile}
\alias{player_skill_profile}
\title{Get a player's stat rating profile with percentile ranks}
\usage{
player_stat_rating_profile(
  player_name,
  ref_date = Sys.Date(),
  seasons = TRUE,
  params = NULL,
  skills = NULL
)

player_skill_profile(
  player_name,
  ref_date = Sys.Date(),
  seasons = TRUE,
  params = NULL,
  skills = NULL
)
}
\arguments{
\item{player_name}{A character string of the player's name (partial OK).}

\item{ref_date}{Date to estimate stat ratings as of. Default is today.}

\item{seasons}{Seasons to include. Numeric vector or TRUE for all.}

\item{params}{Hyperparameters from \code{default_stat_rating_params()}.}

\item{skills}{Optional pre-computed stat ratings data (e.g. from
\code{load_player_stat_ratings(TRUE)}). If provided, skips the expensive
data loading and estimation steps. If NULL (default), computes from scratch.}
}
\value{
A list of class \code{torp_stat_rating_profile} with elements:
\describe{
\item{player_info}{Player ID, name, team, position.}
\item{skills}{Data.frame of stat rating estimates with percentile ranks.}
\item{ref_date}{Reference date used.}
}
}
\description{
Resolves a player by name (partial match OK), estimates stat ratings for all
players, then returns the target player's row with within-position
percentile ranks appended.
}
