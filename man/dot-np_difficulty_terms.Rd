% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_difficulty_terms}
\alias{.np_difficulty_terms}
\title{Difficulty terms for every scorable disposal: p, decision, surprise}
\usage{
.np_difficulty_terms(pbp_data, chains, leak_safe = TRUE)
}
\arguments{
\item{pbp_data}{Play-by-play carrying \code{exp_pts} and \code{delta_epv}.}

\item{chains}{Raw chains for the same matches.}

\item{leak_safe}{Fit each season on strictly earlier seasons.}
}
\value{
\code{match_id}, \code{display_order}, \code{p_hat}, \code{decision}, \code{surprise}.
}
\description{
The identity each disposal follows (docs/plans/EPV-V4-CREDIT-RULES.md, D5-D7):
}
\details{
\if{html}{\out{<div class="sourceCode">}}\preformatted{delta_epv = (EV - before)    the DECISION  -> the disposer
          + (after - EV)     the SURPRISE  -> whoever resolved it
}\if{html}{\out{</div>}}

with \code{EV = (1 - p) * V_keep + p * V_lose} and \code{p} the modelled chance of
losing the ball. Everything here comes from \code{torp/R/epv_difficulty.R}, built
in 2026-08 and never switched on; this is the first consumer. The models are
fitted \strong{leak-safe by season} -- each season scored on earlier seasons --
and fall back to an in-sample fit, loudly, when no earlier season is present.

Terms are in the ACTING team's frame, like \code{delta_epv}; the ledger flips
them to the home-margin frame when it joins them.
}
\keyword{internal}
