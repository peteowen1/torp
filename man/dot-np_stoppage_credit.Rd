% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_stoppage_credit}
\alias{.np_stoppage_credit}
\title{Pay a stoppage swing: rucks, the first-possession player, both pools (D15)}
\usage{
.np_stoppage_credit(
  l,
  lineup,
  loser_share = NP_STOPPAGE_LOSER_SHARE,
  split = NP_STOPPAGE_SPLIT
)
}
\arguments{
\item{l}{Output of \code{.np_credit_terms()}.}

\item{lineup}{Roster with \code{hitouts_to_advantage}, \code{ruck_contests}, \code{hitouts}.}

\item{loser_share}{\code{NP_STOPPAGE_LOSER_SHARE}.}

\item{split}{\code{NP_STOPPAGE_SPLIT}.}
}
\value{
\code{individual} (\code{match_id}, \code{team}, \code{player_id}, \code{np_stoppage}),
\code{pool} rows shaped for \code{.np_spread_pool()}, and \code{rows} for the payment
table.
}
\description{
The swing on a stoppage row is baseline -> first possession, in the
home-margin frame. \code{NP_STOPPAGE_LOSER_SHARE} of it is worn by the side that
lost the ball; the rest is credited to the side that won it. Each side's
half splits by how the ball came out (\code{NP_STOPPAGE_SPLIT}): the ruck share
goes to that side's rucks in proportion to \code{hitouts_to_advantage} (winner)
or \code{ruck_contests - hitouts} (loser) that match, the player share to the
first-possession player, the rest to the side's pool. A side with no ruck
credited passes the ruck share to its pool. A stoppage with no first
possession (a stoppage straight into another stoppage) splits its swing
between the two pools.
}
\keyword{internal}
