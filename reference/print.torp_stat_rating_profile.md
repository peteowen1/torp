# Print a player stat rating profile

Print a player stat rating profile

## Usage

``` r
# S3 method for class 'torp_stat_rating_profile'
print(x, ...)

# S3 method for class 'torp_skill_profile'
print(x, ...)
```

## Arguments

- x:

  A `torp_stat_rating_profile` object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
