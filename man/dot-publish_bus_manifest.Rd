% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{.publish_bus_manifest}
\alias{.publish_bus_manifest}
\title{Publish/refresh \code{bus_manifest.json} for one uploaded asset (T13)}
\usage{
.publish_bus_manifest(tag, name, local_path, rows = NULL)
}
\arguments{
\item{tag}{Release tag.}

\item{name}{Asset file name (e.g. \code{"chains_data_2026_all.parquet"}).}

\item{local_path}{Local path to the just-uploaded file (still on disk).}

\item{rows}{Optional row count for the manifest entry.}
}
\value{
Invisible manifest list.
}
\description{
Reads the tag's previous manifest (legacy mode -- NULL -- when absent),
replaces this asset's entry, carries every other entry forward unchanged
(so the manifest always describes the whole tag, per
ECOSYSTEM-FIX-PLAN.md §1.2's partial-tag-publish rule), and uploads the
result as \code{bus_manifest.json} LAST. Called from \code{\link[=save_to_release]{save_to_release()}} after
its own upload has already succeeded and been verified -- this function's
only job is to keep the commit record current, never to gate the upload.
}
\keyword{internal}
