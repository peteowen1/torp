# Reception scaling factor for intercept marks (pos_team == -1 AND mark in PBP)

Reception scaling factor for intercept marks (pos_team == -1 AND mark in
PBP)

## Usage

``` r
EPV_RECV_INTERCEPT_MARK_SCALE
```

## Format

An object of class `numeric` of length 1.
