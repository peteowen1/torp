% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_error_actor}
\alias{.np_error_actor}
\title{Name the player who made an error the ledger cannot see}
\usage{
.np_error_actor(seq)
}
\arguments{
\item{seq}{Output of \code{.np_sequence()} -- the FULL sequence, because the rows
this needs are the ones \code{in_pbp == TRUE} removes.}
}
\value{
\code{match_id}, \code{display_order}, \code{err_pid}, \code{err_desc}; empty if chains
carries none.
}
\description{
The ledger runs on PBP, and 51 of chains' 78 play types never reach it
(80,694 rows in 2026). When one of those names a player at fault and sits
between a disposal and the opposition's next possession, PBP sees only
\verb{Kick (SYD Bice) -> Loose Ball Get (NMFC)}: the team changes, the KICK is
booked as the turnover, and Bice wears the whole swing for a mark Nick
Blakey dropped.
}
\details{
Measured on 2026, \verb{Mark Fumbled} and \verb{Mark Dropped} are blamed on a
different player \strong{100\%} of the time -- 1,449 rows, though \code{Fumbled -> Dropped} is one event on two rows so the distinct count is nearer 787.

Returns at most one row per PBP act: the FIRST qualifying error in the gap,
and only where the erring player is a teammate of the actor and is not the
actor himself. Both conditions matter. A different team means an opponent
fumbled an intercept, which is a different event and not this row's fault;
the same player means the attribution is already right, which is exactly the
case for \verb{Out On Full After Kick} and why it is not in \code{NP_ERROR_DESCS}.
}
\keyword{internal}
