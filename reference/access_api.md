# Access API

This function is intended for internal use and may be unexported in a
future release. Makes an authenticated request to the AFL API.

## Usage

``` r
access_api(url)
```

## Arguments

- url:

  The API endpoint URL.

## Value

The parsed JSON content of the API response.
