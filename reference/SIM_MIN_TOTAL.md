# Minimum combined score floor for simulated matches (points). Prevents unrealistically low totals from extreme rnorm draws.

Minimum combined score floor for simulated matches (points). Prevents
unrealistically low totals from extreme rnorm draws.

## Usage

``` r
SIM_MIN_TOTAL
```

## Format

An object of class `numeric` of length 1.
