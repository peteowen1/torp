% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_player.R
\name{plot_player_rating}
\alias{plot_player_rating}
\title{Plot player rating trend over time}
\usage{
plot_player_rating(
  player_name,
  seasons = TRUE,
  metric = c("torp_value", "epv", "psv"),
  rolling = 5,
  show_season_avg = TRUE,
  data = NULL
)
}
\arguments{
\item{player_name}{Player name (partial match OK, resolved via \code{resolve_player()}).}

\item{seasons}{Seasons to include. Numeric vector or TRUE for all. Default TRUE.}

\item{metric}{One of \code{"torp_value"} (default), \code{"epv"}, \code{"psv"}. The per-game
value column to plot.}

\item{rolling}{Rolling average window (number of games). Default 5.}

\item{show_season_avg}{Logical. If TRUE (default), show horizontal segments
for each season's average.}

\item{data}{Optional pre-loaded data from \code{load_player_game_ratings()}.}
}
\value{
A ggplot2 object.
}
\description{
Shows per-game TORP (or component) ratings as points with a rolling average
smoothed line. Useful for tracking player form and development.
}
