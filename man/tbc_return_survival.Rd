% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/injuries.R
\name{tbc_return_survival}
\alias{tbc_return_survival}
\title{Empirical Return Distribution for TBC Listings}
\usage{
tbc_return_survival(season = NULL)
}
\arguments{
\item{season}{Season year. Defaults to current via \code{\link[=get_afl_season]{get_afl_season()}}.}
}
\value{
A tibble with one row per TBC listing episode:
\code{player}, \code{team}, \code{injury}, \code{first_tbc_round} (round at scrape),
\code{return_round} (first lineup appearance, NA if censored),
\code{rounds_out} (observed, with censoring flag), \code{censored} (logical).
Attached \code{cdf} attribute: offset, returned, total, cumulative P(return).
}
\description{
For every weekly TBC listing in the season's injury history, measures
rounds-until-first-senior-game after the listing's scrape. Returns a
tibble of listing-level observations plus an \code{attr(x, "cdf")} with the
empirical P(returned by +N rounds) and median rounds-to-return (NA if
more than half are still censored).
}
\details{
Players who have not yet returned are right-censored: their observed
rounds-out is a lower bound. The CDF uses a simple observed / total
estimator -- it under-estimates the true return probability at higher
offsets (because some censored players will eventually return), so treat
it as a conservative floor.

Used to audit the \code{parse_return_round("TBC")} default of
\code{current_round + 3} -- if the median actual return is 1-2 rounds, the
simulation is treating TBC as more severe than the data supports.
}
