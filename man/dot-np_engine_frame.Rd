% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_engine_frame}
\alias{.np_engine_frame}
\title{Net points as the v4 EPV engine's player-game frame}
\usage{
.np_engine_frame(
  pbp_data,
  player_stats,
  chains,
  difficulty_terms = NULL,
  results = NULL
)
}
\arguments{
\item{pbp_data}{Play-by-play for the matches to rate.}

\item{player_stats}{Box-score stats for the same matches.}

\item{chains}{Raw chains for the same matches.}

\item{difficulty_terms}{Precomputed terms (leak-safe from
\code{np_difficulty_terms_for_season()}); fitted in-sample, with a warning,
when \code{NULL}.}

\item{results}{Official results, or \code{NULL} to load them for the seasons in
\code{pbp_data} (falling back to the running score when that fails).}
}
\value{
\code{build_net_points()} output under the v4 rule set.
}
\description{
The margin each match comes from the official results where they exist and
from the play-by-play's own running score otherwise, so a match in progress
balances to the score so far. The two disagree more than you would think:
on 2026 the booked running score differed from the official result in 24\%
of matches, by up to 6 points, so the official margin is the one the ledger
is pinned to and the difference sits in the reconciliation residual.
}
\keyword{internal}
