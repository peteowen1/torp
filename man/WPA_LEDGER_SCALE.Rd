% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants_ratings.R
\docType{data}
\name{WPA_LEDGER_SCALE}
\alias{WPA_LEDGER_SCALE}
\title{Units the WPA ledger runs the net points engine in}
\format{
An object of class \code{numeric} of length 1.
}
\usage{
WPA_LEDGER_SCALE
}
\description{
\code{build_wpa_ledger()} feeds win probability change through the net points
engine, which was written for points and has absolute guards sized for
them. Win probability is a fraction, so a whole match moves the ledger by
at most 1; scaling by 100 puts it in percentage points, the same order as a
typical margin, before the engine sees it. Output is divided back, so the
published columns are fractions like \code{wp_credit}.
}
\keyword{internal}
