# Decay factor (in days) for spoil component weighting

Decay factor (in days) for spoil component weighting

## Usage

``` r
RATING_DECAY_SPOIL
```

## Format

An object of class `numeric` of length 1.
