# Generate Disk Cache Key from URL

Creates a unique cache key based on the URL hash.

## Usage

``` r
generate_disk_cache_key(url)
```

## Arguments

- url:

  Character URL to generate cache key for

## Value

Character cache key (filename without extension)
