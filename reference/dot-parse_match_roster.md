# Parse match roster response into a flat tibble

Parse match roster response into a flat tibble

## Usage

``` r
.parse_match_roster(resp, match_id)
```

## Arguments

- resp:

  Parsed JSON from the matchRoster endpoint

- match_id:

  The match providerId

## Value

A tibble with player lineup data
