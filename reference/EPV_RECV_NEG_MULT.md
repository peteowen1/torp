# Reception multiplier when defending (pos_team == -1)

Reception multiplier when defending (pos_team == -1)

## Usage

``` r
EPV_RECV_NEG_MULT
```

## Format

An object of class `numeric` of length 1.
