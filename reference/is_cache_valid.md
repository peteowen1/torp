# Check if cache entry is valid

Check if cache entry is valid

## Usage

``` r
is_cache_valid(cache_entry, cache_ttl)
```

## Arguments

- cache_entry:

  Cache entry object

- cache_ttl:

  Time-to-live in seconds

## Value

Logical indicating if cache is valid
