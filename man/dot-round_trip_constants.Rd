% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratings_version.R
\name{.round_trip_constants}
\alias{.round_trip_constants}
\title{Put a constants list through the exact JSON round-trip the manifest uses}
\usage{
.round_trip_constants(x)
}
\arguments{
\item{x}{A (possibly nested) list.}
}
\value{
The same structure after \code{toJSON()} then \code{fromJSON()}.
}
\description{
\code{read_ratings_manifest()} parses with \code{simplifyVector = FALSE}, and
\code{publish_ratings_manifest()} writes with \verb{auto_unbox = TRUE, null = "null"}.
Comparing live constants to a manifest entry is only meaningful if BOTH
sides went through the identical coercion -- integers becoming doubles,
\code{NA_character_} becoming JSON \code{null} becoming R \code{NULL}, a named atomic
vector becoming a list of scalars. Skipping this step and comparing raw R
structures with \code{identical()} would false-alarm on every run.
}
\keyword{internal}
