% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rapm.R
\name{fit_team_rapm_split}
\alias{fit_team_rapm_split}
\title{Ridge-fit the "split" RAPM design, holding out whole MATCHES per CV fold
(both sides of a match share players via the opp-block columns, so
splitting them across folds would let a player's opp-block presence leak
between train and test).}
\usage{
fit_team_rapm_split(rapm_data, nfolds = 10, seed = 20260825)
}
\arguments{
\item{rapm_data}{Output of \code{build_team_rapm_split()}.}

\item{nfolds}{CV folds. Default 10.}

\item{seed}{RNG seed.}
}
\value{
list(model, lambda_min, cv_r2, in_sample_r2, n, p, n_over_p).
}
\description{
Ridge-fit the "split" RAPM design, holding out whole MATCHES per CV fold
(both sides of a match share players via the opp-block columns, so
splitting them across folds would let a player's opp-block presence leak
between train and test).
}
\keyword{internal}
