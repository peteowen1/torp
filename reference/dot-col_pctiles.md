# Compute percentile rank of a player's values within a reference data.table

Compute percentile rank of a player's values within a reference
data.table

## Usage

``` r
.col_pctiles(dt, cols, player_row)
```
