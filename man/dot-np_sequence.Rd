% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_sequence}
\alias{.np_sequence}
\title{The full act sequence: PBP rows, plus the chains rows PBP drops}
\usage{
.np_sequence(pbp_data, chains = NULL)
}
\arguments{
\item{pbp_data}{Play-by-play.}

\item{chains}{Raw chains for the same matches, or \code{NULL}.}
}
\value{
A data.table ordered by \verb{match_id, display_order} with
\code{description}, \code{team}, \code{home_away}, \code{player_id}, \code{delta_epv} and
\code{in_pbp} (does this row carry a PBP state).
}
\description{
PBP is an exact subset of chains on \verb{(match_id, display_order)} -- measured
on 2026: 355,399 PBP rows, every one present in chains with the same
description, team and player, and 82,718 chains-only rows. Those extra rows
are precisely the ones the credit rules need to see and PBP cannot show:
\code{Spoil} (13,096, names the spoiler), \verb{Contest Target} (4,423, names the
intended target), \code{Goal} / \code{Behind}, \verb{Mark Fumbled}, \code{Tackle}. They carry no
\code{delta_epv} -- the value stays on the PBP row that precedes them -- so they
are visible for \strong{resolution} (what the disposal turned into) without ever
entering the ledger's value.
}
\details{
Without \code{chains} the sequence is PBP alone and every row is \code{in_pbp}.
}
\keyword{internal}
