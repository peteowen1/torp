# Add game variables (Pass 4)

Adds game-level variables grouped by match_id. Modifies the data.table
by reference.

## Usage

``` r
add_game_vars_dt(dt)
```

## Arguments

- dt:

  A data.table to modify

## Value

Invisible NULL (modifies dt by reference)
