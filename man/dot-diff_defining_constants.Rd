% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratings_version.R
\name{.diff_defining_constants}
\alias{.diff_defining_constants}
\title{Diff a manifest vintage's \code{defining_constants} against the live constants}
\usage{
.diff_defining_constants(
  manifest_constants,
  live_constants = .rating_defining_constants()
)
}
\arguments{
\item{manifest_constants}{The canonical vintage's \code{defining_constants}
entry, already JSON-round-tripped by virtue of having come from
\code{read_ratings_manifest()} (or an equivalently round-tripped test fixture).}

\item{live_constants}{The currently loaded constants. Defaults to
\code{.rating_defining_constants()}; injectable for tests.}
}
\value{
A named list of differing leaves, each \code{list(manifest = ..., live = ...)}.
Empty when every leaf agrees.
}
\description{
Both sides are flattened and every leaf normalised through the same JSON
text form, so a leaf present on only one side reports as \code{"<absent>"}
rather than crashing, and a leaf whose VALUE differs (a decay constant
edited without a vintage bump) is reported by name with both values.
}
\keyword{internal}
