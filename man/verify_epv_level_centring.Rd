% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/centring_verification.R
\name{verify_epv_level_centring}
\alias{verify_epv_level_centring}
\title{Verify EPV level centring took}
\usage{
verify_epv_level_centring(
  pgd,
  channels = EPV_LEVEL_CENTRE_CHANNELS,
  cells = attr(pgd, "epv_level_centring"),
  tol = 1e-08,
  shrink_on = isTRUE(EPV_POSITION_SHRINK)
)
}
\arguments{
\item{pgd}{Player-game data returned by \code{centre_epv_by_position()}.}

\item{channels}{Channel stems that were centred.}

\item{cells}{The per-cell record centring left behind; defaults to the
\code{epv_level_centring} attribute of \code{pgd}. Injectable so tests can drive the
shrinkage path without running the centring.}

\item{tol}{Maximum tolerated deviation from the expected residual.}

\item{shrink_on}{Whether shrinkage was active for the run being checked.}
}
\value{
Invisibly, \verb{list(cells = <n cells checked>, worst = <max deviation>)}.
Aborts on any failure.
}
\description{
Checks that the TOG-weighted mean of each centred channel per
\verb{(season, round, position bucket)} is what the centring recorded it should
be. The weighted sum is what EPR's numerator accumulates, so it is the thing
that must vanish -- an unweighted mean would look centred while EPR stayed
skewed.
}
\details{
With \code{EPV_POSITION_SHRINK} on, a cell is deliberately NOT left at zero: it
keeps \verb{(1 - lambda) * (round mean - bucket's earlier mean)}. That is still an
exact quantity, so this compares against the per-cell residual centring
recorded rather than loosening the tolerance to accommodate the feature.
}
\keyword{internal}
