% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_ratings.R
\name{calculate_torp_ratings}
\alias{calculate_torp_ratings}
\alias{torp_ratings}
\title{Get TORP ratings}
\usage{
calculate_torp_ratings(
  season_val = get_afl_season(type = "current"),
  round_val = get_afl_week(type = "next"),
  decay = RATING_DECAY_DEFAULT_DAYS,
  loading = RATING_LOADING_DEFAULT,
  prior_games_recv = RATING_PRIOR_GAMES_RECV,
  prior_games_disp = RATING_PRIOR_GAMES_DISP,
  plyr_tm_df = NULL,
  player_game_data = NULL,
  prior_games_spoil = RATING_PRIOR_GAMES_SPOIL,
  prior_games_hitout = RATING_PRIOR_GAMES_HITOUT,
  fixtures = NULL,
  skills = TRUE
)

torp_ratings(
  season_val = get_afl_season(type = "current"),
  round_val = get_afl_week(type = "next"),
  decay = RATING_DECAY_DEFAULT_DAYS,
  loading = RATING_LOADING_DEFAULT,
  prior_games_recv = RATING_PRIOR_GAMES_RECV,
  prior_games_disp = RATING_PRIOR_GAMES_DISP,
  plyr_tm_df = NULL,
  player_game_data = NULL,
  prior_games_spoil = RATING_PRIOR_GAMES_SPOIL,
  prior_games_hitout = RATING_PRIOR_GAMES_HITOUT,
  fixtures = NULL,
  skills = TRUE
)
}
\arguments{
\item{season_val}{The season to calculate ratings for. Default is the next season.}

\item{round_val}{The round to calculate ratings for. Default is the next round.}

\item{decay}{The decay factor for weighting games. Default is \code{RATING_DECAY_DEFAULT_DAYS}.}

\item{loading}{The loading factor for TORP calculations. Default is \code{RATING_LOADING_DEFAULT}.}

\item{prior_games_recv}{The number of prior games to consider for receiving. Default is \code{RATING_PRIOR_GAMES_RECV}.}

\item{prior_games_disp}{The number of prior games to consider for disposal. Default is \code{RATING_PRIOR_GAMES_DISP}.}

\item{plyr_tm_df}{Optional pre-loaded player team data. If NULL, will load automatically.}

\item{player_game_data}{Optional pre-loaded player game data. If NULL, will load automatically.}

\item{prior_games_spoil}{Prior games for spoil shrinkage. Default is \code{RATING_PRIOR_GAMES_SPOIL}.}

\item{prior_games_hitout}{Prior games for hitout shrinkage. Default is \code{RATING_PRIOR_GAMES_HITOUT}.}

\item{fixtures}{Optional pre-loaded fixtures data. If NULL, will load automatically.}

\item{skills}{Controls TOG-weighted average adjustment. When active, TORP
components are re-centered to "above TOG-weighted average" by subtracting
the weighted mean of each component (weighted by
\code{time_on_ground_skill}). Accepts:
\itemize{
\item \code{TRUE} (default): auto-loads skills via
\code{get_player_skills(current = FALSE)}.
\item A data.frame with \code{player_id} and
\code{time_on_ground_skill} columns.
\item \code{FALSE} or \code{NULL}: skip adjustment.
}
Players not in \code{skills} default to weight 0 (excluded from the
average, but still have the average subtracted).}
}
\value{
A data frame containing player TORP ratings.
}
\description{
This function calculates TORP (Total Overall Rating Points) for players based on their performance.
}
