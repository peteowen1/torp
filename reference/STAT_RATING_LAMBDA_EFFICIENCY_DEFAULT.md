# Default exponential decay rate for efficiency stats (per day) Half-life = ln(2) / 0.0013 ~ 533 days

Default exponential decay rate for efficiency stats (per day) Half-life
= ln(2) / 0.0013 ~ 533 days

## Usage

``` r
STAT_RATING_LAMBDA_EFFICIENCY_DEFAULT

SKILL_LAMBDA_EFFICIENCY_DEFAULT
```

## Format

An object of class `numeric` of length 1.

An object of class `numeric` of length 1.
