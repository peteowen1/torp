% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opponent_profile_core.R
\name{.shrink_to_league}
\alias{.shrink_to_league}
\title{Shrink a team's weighted mean toward the league average}
\usage{
.shrink_to_league(wt_sum, wt_mean, league_avg, prior_games)
}
\arguments{
\item{wt_sum}{Total weight of the team's prior games.}

\item{wt_mean}{The team's weighted mean over those games.}

\item{league_avg}{The weighted league mean to shrink toward.}

\item{prior_games}{Strength of the prior, in units of weight.}
}
\value{
The shrunk value.
}
\description{
\code{(wt_sum * wt_mean + prior_games * league_avg) / (wt_sum + prior_games)}.
Appeared identically at three call sites.
}
\details{
The shrinkage is on the cell's own WEIGHT, not its game count: a team whose
prior games are all heavily decayed carries little evidence, and weight says
that where a count does not. \code{prior_games = 0} reproduces the unshrunk
weighted mean exactly.

Note the ordering with the cap that callers apply afterwards: shrinkage runs
FIRST and does most of the work. On a two-team fixture with a 1-vs-10000
spread, \code{prior_games = 5} alone keeps the resulting factor inside
\code{OPP_ADJ_FACTOR_CAP} -- the cap is a backstop behind this, not the primary
control (see test-opponent-adjustment-profiles.R).
}
\keyword{internal}
