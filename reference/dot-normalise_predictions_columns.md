# Normalise Predictions Column Names

Maps old column names in predictions data to canonical snake_case.

## Usage

``` r
.normalise_predictions_columns(df)
```

## Arguments

- df:

  A data.frame, tibble, or data.table of predictions data.

## Value

The input with normalised column names, modified by reference.
