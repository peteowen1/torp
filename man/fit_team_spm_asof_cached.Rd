% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rapm_asof_cache.R
\name{fit_team_spm_asof_cached}
\alias{fit_team_spm_asof_cached}
\title{Cached as-of SPM fit: memoizes \code{fit_team_spm_asof()}. The RAPM
ratings passed in are content-hashed into the cache key (not assumed
reproducible from \code{ref_date} alone), so a cache hit is only served
when both the SPM call's own arguments AND the RAPM ratings it was given
are unchanged.}
\usage{
fit_team_spm_asof_cached(
  ref_date,
  rapm_asof_ratings,
  comp = "AFLM",
  seasons = TRUE,
  alpha = 0.5,
  nfolds = 10,
  seed = 20260825,
  prior_games = 10
)
}
\arguments{
\item{ref_date}{Date. Determines the training-season cutoff.}

\item{rapm_asof_ratings}{Output of
\code{extract_team_rapm_ratings()} run on this SAME \code{ref_date}'s
\code{build_team_rapm_asof()}/\code{fit_team_rapm_asof()} output -- the
RAPM target this SPM predicts must itself already be point-in-time, or
this function's own leak-safety is moot.}

\item{comp}{"AFLM" (default) or "AFLW".}

\item{seasons}{Passed to \code{load_results()} to enumerate all available
seasons (the pool to restrict to \code{< cutoff_season}).}

\item{alpha}{Elastic-net mixing parameter. Default 0.5.}

\item{nfolds}{CV folds. Default 10; effectively capped at \code{nrow(X)}
after merging \code{rapm_ratings} with \code{spm_features}.}

\item{seed}{RNG seed.}

\item{prior_games}{Passed to \code{shrink_team_rapm()}.}
}
\value{
Identical, cache hit or miss, to calling \code{fit_team_spm_asof()}
directly.
}
\description{
Cached as-of SPM fit: memoizes \code{fit_team_spm_asof()}. The RAPM
ratings passed in are content-hashed into the cache key (not assumed
reproducible from \code{ref_date} alone), so a cache hit is only served
when both the SPM call's own arguments AND the RAPM ratings it was given
are unchanged.
}
\keyword{internal}
