# Parse squad JSON into a player data.frame

Parse squad JSON into a player data.frame

## Usage

``` r
.parse_squad_json(json)
```

## Arguments

- json:

  Parsed JSON from the squads endpoint

## Value

A data.frame of players with team info, or NULL
