# Strip a list of GAM models for compact serialisation

Strip a list of GAM models for compact serialisation

## Usage

``` r
.strip_gam_models(models)
```

## Arguments

- models:

  Named list of gam/bam objects

## Value

Same list with stripped models
