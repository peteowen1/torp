% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{build_net_points}
\alias{build_net_points}
\title{Allocate the actual match margin across players (Net Points)}
\usage{
build_net_points(
  pbp_data = NULL,
  player_stats = NULL,
  results = NULL,
  defensive_share = NP_DEFENSIVE_SHARE,
  receiver_share = NP_RECEIVER_SHARE,
  ball_winner_share = NP_BALL_WINNER_SHARE,
  spread = c("matchup", "defensive_acts", "tog"),
  mirror_share = NP_MIRROR_SHARE,
  level = c("half_margin", "sum"),
  reconcile = TRUE
)
}
\arguments{
\item{pbp_data}{Play-by-play. Defaults to \code{load_pbp(TRUE)}.}

\item{player_stats}{Box-score stats supplying the lineup, positions, time on
ground and defensive acts. Defaults to \code{load_player_stats(TRUE)}.}

\item{results}{Match results supplying the margin. Defaults to
\code{load_results(TRUE)}.}

\item{defensive_share}{Fraction of each turnover paid to the team that won
the ball. Not identifiable from conservation -- see \code{NP_DEFENSIVE_SHARE}.}

\item{receiver_share}{Fraction of a retained disposal paid to the receiver.}

\item{ball_winner_share}{Fraction of each defensive pool paid straight to the
player observed to win the ball (the actor on the next row). The remainder
goes through \code{spread}, for the pressure that forced the turnover. Routing
the whole pool through \code{spread} was measurably wrong -- see
\code{NP_BALL_WINNER_SHARE}.}

\item{spread}{How the defensive pool is divided: \code{"matchup"} (the positional
mirror takes \code{mirror_share}, rest by TOG), \code{"defensive_acts"} (by box-score
defensive work) or \code{"tog"} (flat by time on ground).}

\item{mirror_share}{Share the mirror slot takes when \code{spread = "matchup"}.}

\item{level}{\code{"half_margin"} pins each team's total to half the margin, which
is the Oliver shape; \code{"sum"} pins only the match total and leaves the level
as the raw ledger produced it. See \code{.np_reconcile()} for why the level
needs its own constraint.}

\item{reconcile}{Whether to book the residual so each match sums exactly to
its margin. \code{FALSE} leaves the raw allocation, which is what you want when
measuring how close the ledger gets on its own.}
}
\value{
A data.table with one row per player-match:
\describe{
\item{\code{np_direct}}{value from the player's own acts}
\item{\code{np_defensive_won}}{paid for turnovers he was observed to win}
\item{\code{np_defensive}}{his share of the pressure pools his team won}
\item{\code{np_ceded}}{the part of his own turnover debit that was paid to the
opposition instead of to him. Read the SIGN carefully: this is a
positive number that REDUCES his debit, because the debit itself is
already in \code{np_direct} at full size and this cancels \code{defensive_share}
of it. It is not a penalty column.}
\item{\code{np_residual}}{his share of the unexplained margin}
\item{\code{net_points}}{the total, in the player's OWN team frame, so
positive is always good}
}
Team totals differ by the match margin.
}
\description{
Rebuilds EPV as a conservation ledger rather than a fitted score: the margin
is the fixed total and every point of it is credited or debited to a player.
Summed per match, \code{net_points} differs between the two teams by exactly the
final margin.
}
\details{
This is Dean Oliver's Net Points model stated for AFL. It replaces nothing in
the published pipeline -- \code{get_player_game_ratings()} and EPR are untouched --
and exists so the allocation can be measured before anything is switched over.
}
\section{Do not add a home player's value to an away player's}{

\code{net_points} and every component are reported in each player's OWN team
frame, so positive always means "helped my team". That frame is not shared
between the two teams, so summing across them is meaningless -- the identity
is a DIFFERENCE, \code{sum(home) - sum(away) == margin}. Use \code{net_points_hm} if
you need a single additive frame. The visible symptom of getting this wrong
is that both sides of a zero-sum transfer read positive: a turnover shows up
as a positive \code{np_ceded} for the disposer and a positive \code{np_defensive} for
the opponent, which is correct in own frames and looks like double-counting.
}

\section{What this does not contain}{

No centring, standardisation, time-on-ground scaling or opponent adjustment.
Each compares a player against an expectation, and subtracting an expectation
breaks the identity this function exists to hold. They belong at the EPR
layer. See \code{docs/plans/EPV-NET-POINTS.md} §3 A4.
}

\examples{
\dontrun{
np <- build_net_points()
# the identity, per match:
np[, .(diff = sum(net_points * ifelse(home_away == "Home", 1, -1))), by = match_id]
}
}
