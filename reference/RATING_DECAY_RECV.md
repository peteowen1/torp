# Decay factor (in days) for receiving component weighting

Decay factor (in days) for receiving component weighting

## Usage

``` r
RATING_DECAY_RECV
```

## Format

An object of class `numeric` of length 1.
