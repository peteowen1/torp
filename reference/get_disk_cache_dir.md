# Get Disk Cache Directory

Returns the directory used for persistent disk caching. Creates the
directory if it doesn't exist.

## Usage

``` r
get_disk_cache_dir()
```

## Value

Character string of the cache directory path
