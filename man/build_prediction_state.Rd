% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_model.R
\name{build_prediction_state}
\alias{build_prediction_state}
\title{Build everything a match prediction needs, and publish nothing}
\usage{
build_prediction_state(
  week = NULL,
  weeks = NULL,
  season = NULL,
  refresh_results = TRUE
)
}
\arguments{
\item{week}{Single target week (auto-detected if NULL)}

\item{weeks}{Vector of weeks, or "all" for all fixture weeks}

\item{season}{Season year (default: current via get_afl_season())}

\item{refresh_results}{If TRUE (production default), refresh the season's
results from the AFL API and publish them to the \code{results-data} release
before building. This is the ONE side effect inside the state half; pass
FALSE for a read-only build. \strong{Note what FALSE costs:} \code{results} feeds
\code{.build_team_mdl_df()} and therefore GAM training, so a read-only build
trains on whatever the release already held, with nothing at runtime
saying it was stale. Fine for a same-day dry run, wrong for anything
whose numbers get compared against production.}
}
\value{
A list with \code{season}, \code{target_weeks}, \code{is_backfill}, \code{all_preds},
\code{week_gms}, \code{team_mdl_df}, \code{team_rt_fix_df} (the feature frame \emph{after}
the roster/injury overlay), \code{all_grounds}, \code{gam_result}, \code{xgb_result},
\code{validation_errors} and \code{pipeline_start} — \strong{or \code{NULL}} when there is
nothing to predict (no TORP ratings for the target week yet: pre-season,
or fixtures not published). Callers MUST handle the \code{NULL};
\code{NULL$anything} is \code{NULL} in R, so an unchecked caller reads every field
as empty and fails much later somewhere unrelated.

\code{validation_errors} is non-empty only on an interactive run; a
non-interactive run aborts instead, exactly as the pipeline did before
the split.
}
\description{
The whole of \code{run_predictions_pipeline()} except the uploads: loads data,
builds fixture/rating/injury features, trains the GAM + XGBoost chain,
generates and validates predictions, and hands the lot back.
}
\details{
Split out 2026-08-11. \code{run_predictions_pipeline()} bundled an upload it
could not opt out of, and that single fact caused three separate problems:
\code{build_matchup_table()} re-implemented this sequence rather than call it
(see \code{matchup_table.R}'s own header), the orchestration had no test
coverage because exercising it published, and the margin-calibration
sidecar scored its own copy of the blend. Callers that want the state
without the side effects now have a seam to use.
}
\keyword{internal}
