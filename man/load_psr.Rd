% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_psr}
\alias{load_psr}
\title{Load Player Skill Ratings (PSR)}
\usage{
load_psr(seasons = get_afl_season(), use_disk_cache = FALSE, columns = NULL)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years associated with given AFL
seasons — defaults to latest season. If set to \code{TRUE}, returns all
available data since 2021.}

\item{use_disk_cache}{Logical. If \code{TRUE}, uses persistent disk cache for
faster repeated loads. Default is \code{FALSE}.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}
}
\value{
A data frame containing PSR data with columns including
\code{player_id}, \code{player_name}, \code{season}, \code{round},
\code{pos_group}, \code{psr_raw}, and \code{psr}.
}
\description{
Loads pre-computed PSR (Player Skill Ratings) from the
\href{https://github.com/peteowen1/torpdata}{torpdata repository}.
PSR represents each player's predicted contribution to match margin
based on their skill profile.
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_psr(2024)
  load_psr(TRUE)  # all seasons
})
}
}
\seealso{
\code{\link[=calculate_psr]{calculate_psr()}}, \code{\link[=load_player_stat_ratings]{load_player_stat_ratings()}}, \code{\link[=load_torp_ratings]{load_torp_ratings()}}
}
