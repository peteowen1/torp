# Decay factor (in days) for hitout component weighting

Decay factor (in days) for hitout component weighting

## Usage

``` r
EPR_DECAY_HITOUT
```

## Format

An object of class `numeric` of length 1.
