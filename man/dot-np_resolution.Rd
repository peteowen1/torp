% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_resolution}
\alias{.np_resolution}
\title{What each disposal turned into: the first row after it that is not in flight}
\usage{
.np_resolution(seq)
}
\arguments{
\item{seq}{Output of \code{.np_sequence()}.}
}
\value{
\code{match_id}, \code{display_order}, \code{resolve_desc}, \code{resolve_team},
\code{resolve_player}, \code{resolve_lag}, for disposal rows only.
}
\description{
The same rule \code{build_disposal_events()} uses: skip the annotation rows that
describe the ball mid-air (\code{CHAINS_INFLIGHT_DESCS}) and take the first row
that says what happened -- \code{Spoil}, \verb{Contested Mark}, \verb{Loose Ball Get},
\code{Goal}. This is a different question from adjacency: adjacency asks who
next held a PBP state (the ground-ball winner after a spoil), resolution
asks who ended the contest (the spoiler). Both are needed and they are
allowed to name different people.
}
\details{
Looks at most six rows ahead. A disposal with nothing but in-flight rows in
that window resolves to \code{NA}.
}
\keyword{internal}
