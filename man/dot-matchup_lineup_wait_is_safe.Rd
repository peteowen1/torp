% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matchup_table.R
\name{.matchup_lineup_wait_is_safe}
\alias{.matchup_lineup_wait_is_safe}
\title{Is a later scheduled run guaranteed before this round's earliest bounce?}
\usage{
.matchup_lineup_wait_is_safe(fixtures, week, from = Sys.time())
}
\arguments{
\item{fixtures}{A fixtures frame with \code{round_number}, \code{status},
\code{utc_start_time} (the shape \code{load_fixtures()} returns).}

\item{week}{Target round number.}

\item{from}{Reference "now" (default \code{Sys.time()}; parameterised for
tests).}
}
\value{
\code{TRUE} only when the deadline is known AND a later scheduled run
precedes it. \code{NA} bounce or \code{NA} next-run both resolve to \code{FALSE} --
an undeterminable state is never treated as safe to skip.
}
\description{
The \code{torp_error_lineups_not_published} quiet-skip decision in
\code{build_matchup_table.R} (torp#190): a still-missing team list is routine
ONLY if a later \code{daily-ratings-predictions.yml} cron run
(\code{MATCHUP_TABLE_CRON_SCHEDULE}) will fire before kickoff and get another
chance to see it published. Comparing against the actual next scheduled
occurrence -- rather than a flat "N hours is always safe" threshold --
is deliberate: the gaps between those four weekly runs are not uniform
(see \code{MATCHUP_TABLE_CRON_SCHEDULE}'s docs), so any single number either
misses the routine case or wrongly treats a genuinely last-chance run as
safe.
}
\keyword{internal}
