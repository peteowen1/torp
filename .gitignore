.Rproj.user
.Rhistory
.RData
.Ruserdata
.Rdata
.httr-oauth
.DS_Store
.quarto
inst/doc

# Archive data (old data files)
inst/extdata/archive/

# Orphaned Shiny artifacts
inst/shiny/

# Deployment metadata
data-raw/rsconnect/

# Generated model outputs
data-raw/outputs/

# Rendered explainer HTML (self-contained, ~5MB embedded)
data-raw/explainer/*.html

# Cached model files (from torpmodels)
inst/models/

# Locally-trained stat models awaiting/already published via
# publish_stat_models() (torpmodels stat-models release tag) -- models stay
# out of git, same convention as inst/models/ above. Pre-existing tracked
# files here (extended_stats_kickins*.rds) are untouched by this rule.
data-raw/stat-models/*.rds

# Obsolete scripts
data-raw/archive/

# Temporary files
*.tmp
*.log
*.bak*

# Stat rating pipeline cache (large RDS files)
data-raw/cache-stat-ratings/
data-raw/cache-skills/
nul
_ul
data-raw/debug/
debug/

# Local R2 upload script (contains no secrets but not part of the package)
data-raw/02-models/push_predictions_to_r2.R

# Development files
.vscode/
.idea/

# pkgdown local build
docs/

# Claude Code files
.claude/
.positai

# Cloudflare Wrangler CLI local cache
.wrangler/

# Pipeline run logs (local artifacts, not source)
data-raw/**/log_*.txt
data-raw/03-ratings/career_team_rapm_asof_*.parquet

# testthat writes this on a failing run (incl. deliberate mutation tests)
tests/testthat/testthat-problems.rds
