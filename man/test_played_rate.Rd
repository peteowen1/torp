% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/injuries.R
\name{test_played_rate}
\alias{test_played_rate}
\title{TEST Listing Played-Rate}
\usage{
test_played_rate(season = NULL, round = NULL)
}
\arguments{
\item{season}{Season year. Defaults to current via \code{\link[=get_afl_season]{get_afl_season()}}.}

\item{round}{Optional round number (or vector) to filter to. If \code{NULL},
analyses all rounds with injury history and lineup data available.}
}
\value{
A tibble with one row per TEST listing: \code{round}, \code{player},
\code{team}, \code{injury}, \code{scraped_at}, \code{played} (logical).
}
\description{
For each completed round, finds players listed as "Test" on the weekly
injury list at the latest scrape before the round's first match, and
checks whether they were named in the selected 22 (non-EMERG/SUB). Useful
for validating whether the model's TEST-as-TBC assumption is too harsh.
}
\details{
Requires accumulated injury snapshots from \code{\link[=save_injury_data]{save_injury_data()}} -- returns
an empty tibble if the release predates \code{scraped_at} tracking or if no
TEST listings have been captured yet.
}
