% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xg.R
\name{extract_player_xg_skill}
\alias{extract_player_xg_skill}
\title{Extract player shooting skill from the shot model}
\usage{
extract_player_xg_skill(shot_model = NULL)
}
\arguments{
\item{shot_model}{Optional fitted shot GAM model. If NULL, loads from
torpmodels via \code{\link[=load_model_with_fallback]{load_model_with_fallback()}}.}
}
\value{
A data.table with columns \code{player_id}, \code{player_name}, \code{xg_skill}
(random effect coefficient), \code{xg_skill_se} (standard error), and
\code{n_shots} (number of shots in training data, if available). Sorted by
\code{xg_skill} descending. Returns NULL if extraction fails.
}
\description{
Extracts per-player random effects from the shot GAM model, representing
each player's shooting ability relative to average (controlling for shot
location, play type, and position). Positive values = better than average
conversion.
}
