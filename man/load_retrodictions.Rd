% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_retrodictions}
\alias{load_retrodictions}
\title{Load Retrodictions Data}
\usage{
load_retrodictions(
  seasons = get_afl_season(),
  rounds = TRUE,
  use_disk_cache = FALSE,
  columns = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years - defaults to latest season. If set to \code{TRUE}, returns all available data since 2021.}

\item{rounds}{A numeric vector of round numbers - defaults to all rounds. If set to \code{TRUE}, returns all available rounds.}

\item{use_disk_cache}{Logical. If TRUE, uses persistent disk cache. Default is FALSE.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}
}
\value{
A data frame containing retrodictions with the same columns as \code{\link[=load_predictions]{load_predictions()}}.
}
\description{
Loads retrodictions from the \href{https://github.com/peteowen1/torpdata}{torpdata repository}.
Retrodictions are the current model's predictions for all matches (completed and upcoming),
regenerated each pipeline run. Compare with \code{\link[=load_predictions]{load_predictions()}} which returns locked
pre-game predictions frozen at kickoff.
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_retrodictions(2026)
})
}
}
\seealso{
\code{\link[=load_predictions]{load_predictions()}}, \code{\link[=load_results]{load_results()}}
}
