# Write per-season disk cache for CFS data

Write per-season disk cache for CFS data

## Usage

``` r
.write_season_disk_cache(prefix, season, data)
```

## Arguments

- prefix:

  Cache prefix

- season:

  Season year

- data:

  Data frame to cache
