% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_torp_data.R
\name{load_player_season_ratings}
\alias{load_player_season_ratings}
\title{Load Player Season Ratings Data}
\usage{
load_player_season_ratings(
  seasons = get_afl_season(),
  use_disk_cache = FALSE,
  columns = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years associated with given AFL
seasons — defaults to latest season. If set to \code{TRUE}, returns all
available data since 2021.}

\item{use_disk_cache}{Logical. If \code{TRUE}, uses persistent disk cache for
faster repeated loads. Default is \code{FALSE}.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}
}
\value{
A data frame containing season-total player ratings with columns
including \code{season}, \code{player_id}, \code{player_name}, \code{team}, \code{position},
\code{games}, \code{season_points}, \code{season_recv}, \code{season_disp}, \code{season_spoil},
\code{season_hitout}, and \code{ppg}.
}
\description{
Loads pre-computed season-total TORP ratings from the
\href{https://github.com/peteowen1/torpdata}{torpdata repository}.
This is the output of \code{\link[=player_season_ratings]{player_season_ratings()}} — season totals and PPG
leaderboards per player.
}
\examples{
\donttest{
try({ # prevents cran errors
  load_player_season_ratings(2024)
})
}
}
\seealso{
\code{\link[=player_season_ratings]{player_season_ratings()}}, \code{\link[=load_player_game_ratings]{load_player_game_ratings()}}, \code{\link[=load_torp_ratings]{load_torp_ratings()}}
}
