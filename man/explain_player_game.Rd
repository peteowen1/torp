% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psr.R
\name{explain_player_game}
\alias{explain_player_game}
\title{Explain a Player's Game Value (EPV + PSV Breakdown)}
\usage{
explain_player_game(
  player_id,
  match_id,
  player_stats = NULL,
  season = NULL,
  per80 = TRUE
)
}
\arguments{
\item{player_id}{Player ID string (e.g. \code{"CD_I1001024"}).}

\item{match_id}{Match ID string (e.g. \code{"CD_M20260140102"}).}

\item{player_stats}{Optional pre-loaded player stats data.table. If NULL,
loads from API via \code{load_player_stats()}.}

\item{season}{Season year (used if \code{player_stats} is NULL).}

\item{per80}{Logical. If TRUE, show per-80-minute contributions.
Default TRUE.}
}
\value{
Invisibly returns a list with \code{epv} (EPV summary data.table)
and \code{psv} (PSV per-stat breakdown data.table).
}
\description{
Shows both the possession-value (EPV) and stat-value (PSV) decomposition
for a player's game performance. EPV comes from play-by-play chain data;
PSV comes from applying glmnet coefficients to box-score stats.
}
