# Decay rate (per day) for EPV opponent defensive profile weighting. ~231-day half-life, matching the stat rating opponent decay.

Decay rate (per day) for EPV opponent defensive profile weighting.
~231-day half-life, matching the stat rating opponent decay.

## Usage

``` r
EPV_OPP_LAMBDA_DECAY
```

## Format

An object of class `numeric` of length 1.
