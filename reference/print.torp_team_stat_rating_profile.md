# Print a team stat rating profile

Print a team stat rating profile

## Usage

``` r
# S3 method for class 'torp_team_stat_rating_profile'
print(x, ...)

# S3 method for class 'torp_team_skill_profile'
print(x, ...)
```

## Arguments

- x:

  A `torp_team_stat_rating_profile` object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
