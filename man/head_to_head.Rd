% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contests.R
\name{head_to_head}
\alias{head_to_head}
\title{Head-to-Head Contest History Between Two Players}
\usage{
head_to_head(
  player1,
  player2,
  type = "all",
  seasons = TRUE,
  rounds = TRUE,
  chains = NULL
)
}
\arguments{
\item{player1}{Character string of the first player's name (partial match OK).}

\item{player2}{Character string of the second player's name (partial match OK).}

\item{type}{Contest type: \code{"aerial"}, \code{"ground_ball"},
or \code{"all"} (default).}

\item{seasons}{Seasons to search. Numeric vector or \code{TRUE} for all.}

\item{rounds}{Rounds to include. Numeric vector or \code{TRUE} for all.}

\item{chains}{Optional pre-loaded chains data to avoid re-downloading.}
}
\value{
An S3 object of class \code{torp_head_to_head} with elements:
\describe{
\item{player1}{List with \code{player_id}, \code{player_name}, \code{team}.}
\item{player2}{List with \code{player_id}, \code{player_name}, \code{team}.}
\item{contests}{data.table of all matchups between the two players.}
\item{summary}{data.table with win/loss record per contest type.}
}
}
\description{
Retrieves the contest history between two specific players across their
career matchups. Uses fuzzy name matching via \code{resolve_player()}.
}
\examples{
\donttest{
try({
  h2h <- head_to_head("Harris Andrews", "Charlie Curnow", type = "aerial")
  print(h2h)
})
}
}
