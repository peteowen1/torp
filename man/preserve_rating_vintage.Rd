% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratings_version.R
\name{preserve_rating_vintage}
\alias{preserve_rating_vintage}
\title{Preserve the current canonical ratings under a vintage label}
\usage{
preserve_rating_vintage(label, repo = get_torp_data_repo(), dry_run = TRUE)
}
\arguments{
\item{label}{Vintage label to preserve the current canonical file as,
e.g. \code{"v1"}.}

\item{repo}{Data repo. Defaults to \code{get_torp_data_repo()}.}

\item{dry_run}{If TRUE (default), report what would happen and upload
nothing. \strong{Deliberately defaults to TRUE}: this function writes to a
release, and a function that publishes by default is one that publishes
by accident.}
}
\value{
Invisibly, a list describing the action.
}
\description{
The one irreversible-by-omission step in the whole versioning scheme. Once
\code{run_ratings_pipeline.R} overwrites \code{torp_ratings.parquet} under new
constants, the previous vintage is gone unless a copy was taken first —
and D-DEF3's entire purpose is that a published number stays traceable to
the definition that produced it.
}
\details{
Run this BEFORE regenerating when promoting a new vintage directly into
canonical (the "switch straight away" path, as opposed to publishing a
candidate alongside and promoting after a soak).
}
\keyword{internal}
