% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratings_version.R
\name{.flatten_named_list}
\alias{.flatten_named_list}
\title{Recursively flatten a nested named list into single-level leaves}
\usage{
.flatten_named_list(x, prefix = "")
}
\arguments{
\item{x}{A (possibly nested) list.}

\item{prefix}{Internal recursion accumulator.}
}
\value{
A flat named list; every element is a leaf value (which may itself
be \code{NULL} -- a JSON \code{null} round-trips to an explicit NULL
element, not a dropped one, because assignment here always uses
single-bracket \verb{[<-} with a length-1 list on the right, which is the one
assignment form that does not delete a NULL from a list).
}
\description{
Leaf names are dotted paths (e.g. \code{"LINEUP_POSITION_GROUP_MAP.CHF"}), which
is what makes a manifest entry that survived a JSON round-trip (see
\code{.round_trip_constants()}) comparable to the live constants leaf by
leaf, regardless of how deep the nesting is.
}
\keyword{internal}
