# Combined position map for match models (structural pairs/trios)

Combined position map for match models (structural pairs/trios)

## Usage

``` r
MATCH_COMBO_POS_MAP
```

## Format

An object of class `list` of length 8.
