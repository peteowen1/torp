% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aflw_season_snapshots.R
\name{diff_aflw_season_snapshots}
\alias{diff_aflw_season_snapshots}
\title{Difference two AFLW season-stat snapshots into a round-window delta}
\usage{
diff_aflw_season_snapshots(
  earlier,
  later,
  recompute_averages = TRUE,
  drop_unplayed = FALSE
)
}
\arguments{
\item{earlier, later}{Data frames of the SAME season's snapshot, \code{earlier}
captured before \code{later}. Both need \code{player_id} and the cumulative stat
columns; typically the output of \code{load_aflw_season_stats_snapshot()}.}

\item{recompute_averages}{Logical. When \code{TRUE} (default), adds an \verb{_avg}
column per cumulative stat, computed as the differenced total divided by
the differenced \code{games_played} -- the per-game rate WITHIN the window.
Players with zero games in the window get \code{NA} rather than a divide-by-zero.}

\item{drop_unplayed}{Logical. When \code{TRUE}, players with no games in the
window are dropped. Default \code{FALSE}, which keeps them with explicit zero
deltas -- "she played no games" and "she is missing from the data" are
different facts and the caller should be able to tell them apart.}
}
\value{
A data.table: identity columns, \code{games_played} (games in the window),
one delta column per cumulative stat, optional recomputed \verb{_avg} columns,
and \code{snapshot_from}/\code{snapshot_to} marking the window. Non-cumulative source
columns are dropped. Attributes carry what the numbers alone cannot show:
\code{"rate_cols_dropped"} (excluded as non-cumulative),
\code{"schema_cols_dropped"} (cumulative but absent from \code{earlier}, so not
differenceable), \code{"gap_days"} (elapsed days between snapshots -- a pair
spanning much more than a week has probably swallowed a skipped capture
and covers two rounds), \code{"players_changed_team"} (traded mid-window, so
their deltas are attributed wholly to the later club),
\code{"n_players_new_since_earlier"} and \code{"n_players_unplayed_in_window"}.
}
\description{
Given the same season's cumulative season-to-date totals captured at two
different dates, returns what each player accumulated BETWEEN them -- i.e.
the contribution of whatever games fell in that window.
}
\details{
Pure by design: it takes two data frames and touches no network, so the
differencing logic is testable without a release or an API. Use
\code{load_aflw_season_stats_snapshot()} to fetch the frames.
}
