# Position-group quantile adjustment — legacy alias for backwards compatibility

Position-group quantile adjustment — legacy alias for backwards
compatibility

## Usage

``` r
EPV_POS_ADJ_QUANTILE
```

## Format

An object of class `numeric` of length 1.
