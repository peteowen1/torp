% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versebus.R
\name{.error_is_absent}
\alias{.error_is_absent}
\title{Did this download error mean "absent"?}
\usage{
.error_is_absent(e, url)
}
\arguments{
\item{e}{The condition raised by the failed read.}

\item{url}{The URL that was being read.}
}
\value{
\code{TRUE} if the resource is confirmed absent.
}
\description{
Classifies a failed parquet read as absent-vs-transient.

WHY THIS IS NOT JUST A REGEX. The message check is a fast path, not the
authority. R's \code{download.file()} route reports the HTTP status in a
\emph{warning} ("HTTP status was '404 Not Found'") and leaves the error itself
as a bare "cannot open URL '...'", so a message regex sees no 404 and
silently classifies a genuinely missing file as transient. That misrouted
every caller relying on \code{vb_error_absent}: building a brand-new rating
vintage aborted six times over, because the not-yet-written
torp_ratings_\if{html}{\out{<label>}}.parquet read as a network failure rather than as the
legitimately-absent file it was (2026-08-18, staging EPV v3).

Widening the regex would just move the brittleness, so absence is
confirmed against the resource itself.
}
\keyword{internal}
