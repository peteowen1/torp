# Reception multiplier when possessing (pos_team == 1)

Reception multiplier when possessing (pos_team == 1)

## Usage

``` r
EPV_RECV_POS_MULT
```

## Format

An object of class `numeric` of length 1.
