% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ladder.R
\name{simulate_afl_season}
\alias{simulate_afl_season}
\title{Simulate a full AFL season}
\usage{
simulate_afl_season(
  season,
  n_sims = SIM_DEFAULT_N,
  team_ratings = NULL,
  fixtures = NULL,
  predictions = NULL,
  injuries = NULL,
  team_residuals = "auto",
  models = NULL,
  seed = NULL,
  verbose = TRUE,
  keep_games = FALSE,
  n_cores = max(1L, parallel::detectCores() - 1L, na.rm = TRUE)
)
}
\arguments{
\item{season}{Numeric season year (e.g. 2026).}

\item{n_sims}{Number of simulations to run (default \link{SIM_DEFAULT_N}).}

\item{team_ratings}{Optional data.table with \code{team} and \code{torp} columns. If
NULL, loads from torpdata.}

\item{fixtures}{Optional fixture data. If NULL, loads from torpdata.}

\item{predictions}{Optional predictions data with \code{pred_xtotal}.}

\item{injuries}{Injury data.frame from \code{\link[=get_all_injuries]{get_all_injuries()}}. By default,
fetched automatically from the AFL injury list. Pass \code{FALSE} to disable
injury-aware simulation, or supply your own data.frame.}

\item{team_residuals}{Team quality residuals from the match GAM random
effects. \code{"auto"} (default) extracts from the xscore_diff GAM via
\code{\link[=.extract_team_residuals]{.extract_team_residuals()}}. Pass a data.frame with \code{team},
\code{residual_mean}, \code{residual_se} to supply custom values, or \code{NULL} to
disable. Residuals are sampled from \code{N(mean, se)} once per simulation,
capturing coaching/system quality not explained by player TORP.}

\item{models}{Optional named list of GAM models from
\code{run_predictions_pipeline()$models}. When provided, team residuals are
extracted from the fresh \code{xscore_diff} model instead of the stored
torpmodels version. Ignored when \code{team_residuals} is not \code{"auto"}.}

\item{seed}{Optional random seed for reproducibility.}

\item{verbose}{Logical; if TRUE, shows a progress bar.}

\item{keep_games}{Logical; if TRUE, stores per-sim game results (memory
intensive). Default FALSE.}

\item{n_cores}{Number of cores for parallel execution. Defaults to
\code{parallel::detectCores() - 1} (leaving one core free for the OS/session).
Set to \code{1L} for sequential execution.
Values > 1 use \code{\link[parallel:clusterApply]{parallel::parLapply()}} with reproducible L'Ecuyer-CMRG
random streams. Progress bars are not shown in parallel mode.}
}
\value{
An S3 object of class \code{"torp_sim_results"} containing \code{season},
\code{n_sims}, \code{ladders}, \code{finals}, \code{games} (if requested), and
\code{original_ratings}.
}
\description{
Runs multiple Monte Carlo simulations of the AFL regular season and finals
to estimate ladder positions, finals probabilities, and premiership odds.
}
\examples{
\dontrun{
try({
  results <- simulate_afl_season(2025, n_sims = 10, seed = 42)
  print(results)
})
}
}
