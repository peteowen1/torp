% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_model.R
\name{add_weather_to_preds}
\alias{add_weather_to_preds}
\title{Add weather to a predictions tibble}
\usage{
add_weather_to_preds(preds, raw_cols = FALSE)
}
\arguments{
\item{preds}{Predictions tibble with \code{match_id}, \code{season}, \code{round} columns
(typically the \verb{$predictions} slot of \code{run_predictions_pipeline()}, or
the output of \code{load_predictions()}).}

\item{raw_cols}{If \code{TRUE}, also include raw \code{temp_avg}, \code{wind_avg},
\code{precipitation_total}, \code{humidity_avg}, and \code{is_roof} columns.}
}
\value{
\code{preds} with a \code{weather} column (and optionally raw weather
columns) joined by \code{match_id}.
}
\description{
Joins historical or forecast weather onto an existing predictions tibble
by \code{match_id}. Adds a compact \code{weather} summary string (e.g.
\code{"18\\u00B0C, wind 22kph, rain 2.4mm"}, or \code{"Indoor"} for roofed games),
positioned after \code{venue}. Optionally also returns raw weather columns.
}
\details{
For completed matches, weather comes from the Open-Meteo archive via the
torpdata \code{weather-data} release. For upcoming matches, the Open-Meteo
forecast API is queried per venue.
}
\examples{
\dontrun{
result <- run_predictions_pipeline()
result$predictions |>
  dplyr::filter(season == 2026, round == 6) |>
  add_weather_to_preds() |>
  print()
}
}
