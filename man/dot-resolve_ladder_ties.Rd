% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ladder.R
\name{.resolve_ladder_ties}
\alias{.resolve_ladder_ties}
\title{Break ties within an AFL ladder using the official Reg 2.5(c) chain}
\usage{
.resolve_ladder_ties(ladder, played_games)
}
\arguments{
\item{ladder}{A data.table with \code{team}, \code{ladder_points}, \code{percentage},
already sorted by \verb{-ladder_points, -percentage}.}

\item{played_games}{A data.table of completed games with \code{home_team},
\code{away_team}, \code{home_score}, \code{away_score}.}
}
\value{
\code{ladder}, re-ordered so any tied block satisfies the full
tiebreak chain.
}
\description{
\code{ladder} must already be sorted by \verb{-ladder_points, -percentage}. Any
maximal block of 2+ consecutive teams tied on BOTH is re-ordered by: (A)
head-to-head premiership points from just the H&A meetings between the
tied clubs; (B) if still tied, head-to-head percentage from those same
meetings; (C) if still tied, drawn by lot. AFL is not round-robin (each
club plays 23 of 17 possible opponents, some twice, some once, some
zero), so a pair of tied clubs may have 0, 1, or 2 meetings -- whichever
actually happened are summed. Ported from the same rule
\code{inthegame-blog/afl/season-sim.js}'s \code{rankLadder()} implements (torp#106);
here as a one-pass \code{setorder()} (h2h_pts, h2h_pct, lot) rather than that
function's recursive mini-table, since a single per-team random \code{lot}
draw up front is sufficient to fully resolve a tied block in one sort.
}
\details{
"Drawn by lot" (Reg 2.5(c)(iv)(C)) is implemented as a \code{stats::runif()}
draw per team in the residual tied block -- callers that need
reproducible output across repeated calls with identical \code{games_dt}
(e.g. the season Monte Carlo, which already wraps its own call in
\code{withr::local_seed()}/\code{set.seed()}) should seed beforehand. A genuine
full tie through percentage is vanishingly rare in real results but not
negligible in simulation (exact-tie odds are higher over integer-sampled
scores) -- see the rules doc's severity note.
}
\keyword{internal}
