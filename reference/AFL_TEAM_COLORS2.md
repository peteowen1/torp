# Secondary team colours (hex) for AFL teams

Named character vector mapping canonical team names to secondary brand
colour. Useful for contrast in two-tone plots (e.g., home vs away
fills).

## Usage

``` r
AFL_TEAM_COLORS2
```

## Format

An object of class `character` of length 18.
