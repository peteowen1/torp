# Add score variables (Pass 5)

Adds final score-related variables grouped by match_id, period, and
tot_goals. Modifies the data.table by reference.

## Usage

``` r
add_score_vars_dt(dt)
```

## Arguments

- dt:

  A data.table to modify

## Value

Invisible NULL (modifies dt by reference)
