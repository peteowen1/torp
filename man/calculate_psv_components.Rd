% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psr.R
\name{calculate_psv_components}
\alias{calculate_psv_components}
\title{Calculate PSV with Offensive/Defensive Decomposition}
\usage{
calculate_psv_components(
  player_stats,
  coef_df,
  osr_coef_df,
  dsr_coef_df,
  tog_adjust = TRUE,
  center = TRUE
)
}
\arguments{
\item{player_stats}{A data.table/data.frame of per-game player data with
raw stat columns (e.g. \code{goals}, \code{kicks}, \code{disposals}) and
a \code{tog} column (time-on-ground as a fraction 0-1).}

\item{coef_df}{A data.frame with columns \code{stat_name} and \code{beta}
(same format as for \code{calculate_psr()}). If an \code{sd} column is
present, raw rates are divided by SD before applying betas.}

\item{osr_coef_df}{Coefficient data.frame for the offensive model.}

\item{dsr_coef_df}{Coefficient data.frame for the defensive model.}

\item{tog_adjust}{Logical. If TRUE (default), divide raw counts by TOG to
get per-full-game rates (matching the scale the coefficients were trained
on). If FALSE, use raw counts directly.}

\item{center}{Logical. If TRUE (default), subtract the per-round league mean
so PSV represents contribution above the average player that round.}
}
\value{
A data.table with identifier columns plus \code{psv_raw},
\code{psv}, \code{osv}, \code{dsv}.
}
\description{
Applies offensive and defensive coefficient models to per-game stats,
producing \code{psv}, \code{osv}, and \code{dsv} columns where
\code{osv + dsv = psv}.
}
