% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_v3.R
\name{fit_contest_models}
\alias{fit_contest_models}
\title{Fit the three contest branch models}
\usage{
fit_contest_models(cst, train_idx = rep(TRUE, nrow(cst)))
}
\arguments{
\item{cst}{Contest table from \code{build_aerial_contests()}.}

\item{train_idx}{Logical vector selecting the rows to fit on.}
}
\value{
A list of three \code{bam} fits.
}
\description{
\code{p} = P(defence wins), and the two branch values \code{V_att} /
\code{V_def} = \code{E[post-event state | that side won]}. \code{exp_pts} (the
pre-kick state) is a legitimate and by far the strongest feature: it describes
the situation BEFORE the kick, nothing about how the contest resolved. Without
it the fit missed badly in the tails while reading fine through the middle.
}
\keyword{internal}
