% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_rapm_asof_cache.R
\name{clear_team_rapm_asof_cache}
\alias{clear_team_rapm_asof_cache}
\title{Delete cached as-of snapshots. Needed after: a source-data refresh that
the freshness signal didn't happen to change (e.g. a corrected historical
row with the same match count and max date -- rare but possible), or any
edit to the fitting logic that didn't bump \code{.TEAM_RAPM_ASOF_CACHE_VERSION}.}
\usage{
clear_team_rapm_asof_cache(comp = NULL)
}
\arguments{
\item{comp}{Optional -- restrict to one comp's files. \code{NULL} (default)
clears every cached file.}
}
\value{
Invisibly, the number of files removed.
}
\description{
Delete cached as-of snapshots. Needed after: a source-data refresh that
the freshness signal didn't happen to change (e.g. a corrected historical
row with the same match count and max date -- rare but possible), or any
edit to the fitting logic that didn't bump \code{.TEAM_RAPM_ASOF_CACHE_VERSION}.
}
\keyword{internal}
