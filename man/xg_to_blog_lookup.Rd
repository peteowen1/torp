% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_blog.R
\name{xg_to_blog_lookup}
\alias{xg_to_blog_lookup}
\title{Convert xG release / live data into a blog-ready xscore lookup}
\usage{
xg_to_blog_lookup(xg, season_val)
}
\arguments{
\item{xg}{Output of \code{\link[=load_xg]{load_xg()}} (single season) or \code{\link[=get_xg]{get_xg()}} (single
round). Must contain \code{match_id}, \code{home_team}, \code{away_team}, \code{home_xscore},
\code{away_xscore}.}

\item{season_val}{Integer season to stamp onto every row — \code{load_xg()} does
not emit a \code{season} column and match_id parsing alone would hide schema
bugs if the caller mixes seasons by accident.}
}
\value{
Tibble with canonical lookup columns, or \code{NULL} if \code{xg} is empty.
}
\description{
Takes the match-level output of \code{\link[=load_xg]{load_xg()}} or \code{\link[=get_xg]{get_xg()}} and reshapes it
into the \verb{(season, round, home_team, away_team, xscore_home, xscore_away)}
shape that \code{\link[=format_predictions_blog]{format_predictions_blog()}} expects as its \code{xg} argument.
}
\details{
Team names are normalised via \code{\link[=torp_replace_teams]{torp_replace_teams()}} so that live
\code{\link[=get_xg]{get_xg()}} output (raw \code{home_team_name}) joins cleanly against predictions
that already went through normalisation.
}
