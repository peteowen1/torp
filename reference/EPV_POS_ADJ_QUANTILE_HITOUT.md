# Position-group quantile adjustment for hitout component

Position-group quantile adjustment for hitout component

## Usage

``` r
EPV_POS_ADJ_QUANTILE_HITOUT
```

## Format

An object of class `numeric` of length 1.
