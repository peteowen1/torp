% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratings_version.R
\name{check_vintage_alignment}
\alias{check_vintage_alignment}
\title{Refuse a ratings write when the running code disagrees with what is
published as canonical}
\usage{
check_vintage_alignment(
  strict = .strict_mode(),
  manifest = read_ratings_manifest(),
  candidate = NULL
)
}
\arguments{
\item{strict}{If TRUE, an unreadable manifest or an undefined canonical
vintage aborts rather than warning. Defaults to \code{VERSEBUS_STRICT=1} via
\code{.strict_mode()}, matching every other pipeline entry point's convention
(versebus §1.5). Until 2026-08-11 this tested \code{nzchar()} instead, so it
was the one site where \code{VERSEBUS_STRICT="0"} still went strict.
Production call sites pass \code{TRUE} explicitly regardless of the
environment.}

\item{manifest}{The ratings manifest to check against. Defaults to
\code{read_ratings_manifest()}; injectable so this function's tests run
offline.}

\item{candidate}{Vintage label when the run is writing a CANDIDATE vintage
(\verb{torp_ratings_<label>.parquet}) rather than canonical, or \code{NULL} for a
canonical write. A candidate never touches canonical, so drift from
canonical's published constants is not a reason to refuse it -- checking it
anyway made this guard block the exact remedy its own error message
recommends. Still refused for a candidate: an unreadable manifest, and a
label equal to the canonical one, which is a canonical write wearing a
candidate flag.}
}
\value{
Invisibly, \code{list(aligned = TRUE, canonical = canon)} on success, or
\code{list(aligned = NA)} / \code{list(aligned = NA, canonical = canon)} when a
non-strict run grandfathers a gap it could not verify.
}
\description{
The border check \code{preserve_rating_vintage()} and \code{publish_ratings_manifest()}
set up but never enforced: this is the choke point that would have stopped
the 2026-07-27/28 incident on day one. Three checks, each a distinct failure
mode:
}
\details{
\enumerate{
\item The manifest itself must be readable (or the run must be non-strict and
accept the grandfather case of a pre-manifest release).
\item The manifest's \code{canonical} label must match the running code's
\code{RATING_VINTAGE} -- a mismatch means a write would silently relabel rows
(incident 1, exactly).
\item The canonical vintage's recorded \code{defining_constants} must match the
live constants after a JSON round-trip -- catching the subtler failure
where a rating-defining constant was edited without bumping
\code{RATING_VINTAGE}, so the label still matches while the maths changed.
}
}
\keyword{internal}
