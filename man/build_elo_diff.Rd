% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/team_elo.R
\name{build_elo_diff}
\alias{build_elo_diff}
\title{Convenience wrapper: build the Elo table from team_mdl_df and join
elo_diff back onto it in one call.}
\usage{
build_elo_diff(
  team_mdl_df,
  k = ELO_K,
  hga = ELO_HGA,
  carryover = ELO_CARRYOVER
)
}
\arguments{
\item{team_mdl_df}{Complete model dataset from \code{.build_team_mdl_df()}}

\item{k}{Update rate (higher = faster-moving ratings). Default
\code{ELO_K} (tuned via grid search on pre-2025 data).}

\item{hga}{Home-ground advantage, in Elo points, added to the home team's
rating before computing expected result. Default \code{ELO_HGA}.}

\item{carryover}{Fraction of a team's rating retained across a season
boundary (1 = no regression, 0 = full reset to 1500). Default
\code{ELO_CARRYOVER}.}
}
\value{
team_mdl_df with an added \code{elo_diff} column
}
\description{
Convenience wrapper: build the Elo table from team_mdl_df and join
elo_diff back onto it in one call.
}
\keyword{internal}
