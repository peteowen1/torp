% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_skills.R
\name{get_player_stat_ratings}
\alias{get_player_stat_ratings}
\alias{get_player_skills}
\title{Get player stat rating estimates from pre-computed data}
\usage{
get_player_stat_ratings(
  player_name = NULL,
  ref_date = NULL,
  seasons = TRUE,
  current = TRUE
)

get_player_skills(
  player_name = NULL,
  ref_date = NULL,
  seasons = TRUE,
  current = TRUE
)
}
\arguments{
\item{player_name}{A character string of the player's name (partial OK).
If NULL (default), returns all players.}

\item{ref_date}{Optional date to filter to the latest snapshot at or before
this date. If NULL, uses the latest available snapshot.}

\item{seasons}{Seasons to include. Numeric vector or TRUE for all.}

\item{current}{If TRUE (default), only return players on a current team
(i.e. those with a stat rating estimate in the latest season). Set FALSE to
include all historical players.}
}
\value{
A data.table of stat rating estimates -- one row per player.
}
\description{
A fast convenience function that looks up stat rating estimates from the
pre-computed data stored in torpdata releases. When called with a player
name, returns one row for that player. When called without arguments,
returns the latest snapshot for every player.
}
\seealso{
\code{\link[=player_stat_rating_profile]{player_stat_rating_profile()}} for full profile with percentile ranks,
\code{\link[=load_player_stat_ratings]{load_player_stat_ratings()}} to load raw pre-computed data.
}
