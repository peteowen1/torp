# Parse match player stats response

Parse match player stats response

## Usage

``` r
.parse_match_stats(resp, match_id)
```

## Arguments

- resp:

  Parsed JSON from the playerStats endpoint

- match_id:

  The match providerId

## Value

A tibble with player stat data
