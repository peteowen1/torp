% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_credit.R
\name{.frame_epv_engine}
\alias{.frame_epv_engine}
\title{Read the engine a frame was priced with}
\usage{
.frame_epv_engine(x, what = "frame", configured = EPV_ENGINE)
}
\arguments{
\item{x}{A frame produced by \code{create_player_game_data()}, possibly after
transforms that dropped attributes.}

\item{what}{Label used in the warning to say which frame lost its stamp.}

\item{configured}{The engine the package is configured for. An argument
rather than a direct read of the constant so the warning path is testable
without rewriting a locked namespace binding; production never passes it.}
}
\value{
The engine string, or NULL. NULL is the v2 answer.
}
\description{
The single place the \code{epv_engine} attribute is read. There were three
(\code{analyze_match.R}, \code{centre_epv_by_position()}, \code{adjust_epv_for_opponents()}),
each spelling out \code{attr(x, "epv_engine")} and each independently responsible
for noticing when it came back NULL. None did.
}
\details{
\strong{Why an attribute at all, and why it is a trap.} \code{create_player_game_data()}
stamps the engine onto the frame it returns, and R drops attributes on
\code{merge()}, \code{rbind()} and most dplyr verbs -- which is why
\code{adjust_epv_for_opponents()} has to re-attach it by hand after its own merge,
and why the v3 experiment scripts in \verb{data-raw/04-analysis/} call
\code{setattr(d, "epv_engine", "v3")} six-plus times. A frame that loses the stamp
is priced as v2 with nothing said.

\strong{This does not change what is returned} -- NULL still means v2, exactly as
before. It makes the fallback AUDIBLE, and only in the case where it is
wrong: when \code{EPV_ENGINE} is \code{"v3"} a missing stamp is almost certainly a lost
attribute rather than a genuine v2 frame. While \code{EPV_ENGINE} is \code{"v2"} a
missing stamp and a v2 stamp mean the same thing, so warning would be noise.

The fuller fix -- carrying the engine as a column, which survives every
transform -- WAS done on 2026-08-18, reversing the earlier decision recorded
here. The reason it was deferred (adding a column changes \code{player_game_data}'s
released schema) was real but was outweighed: the attribute does not survive
the parquet round-trip the frame makes through the release, so Stage 3 reloaded
it unstamped and priced v3 data as v2 while this function only warned. See
\code{create_player_game_data()} for the column write and
\code{.restore_epv_engine_attr()} for the read-back.
}
\keyword{internal}
