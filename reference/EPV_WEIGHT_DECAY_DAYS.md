# Decay factor (in days) for PBP-level game recency weighting in add_epv_vars()

Decay factor (in days) for PBP-level game recency weighting in
add_epv_vars()

## Usage

``` r
EPV_WEIGHT_DECAY_DAYS
```

## Format

An object of class `numeric` of length 1.
