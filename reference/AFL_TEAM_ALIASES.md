# Named vector mapping team name variants to canonical names

Maps abbreviations, full names, nicknames, Indigenous round names, and
other variants to the canonical short name used in `AFL_TEAMS$name`.

## Usage

``` r
AFL_TEAM_ALIASES
```

## Format

An object of class `character` of length 75.
