% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_model.R
\name{.build_locked_predictions}
\alias{.build_locked_predictions}
\title{Merge new-week predictions into the locked full-season predictions file}
\usage{
.build_locked_predictions(
  pred_file_name,
  season,
  week_gms,
  team_mdl_df,
  target_weeks,
  completed_margins
)
}
\arguments{
\item{pred_file_name}{Base file name (no extension), e.g. "predictions_2026".}

\item{season}{Season year.}

\item{week_gms}{This run's freshly-computed predictions for \code{target_weeks}.}

\item{team_mdl_df}{Full model data frame (used to find already-started matches).}

\item{target_weeks}{Numeric vector of round numbers being (re)computed.}

\item{completed_margins}{Data frame of match_id/.actual_margin for completed matches.}
}
\value{
The combined data frame to upload.
}
\description{
Encapsulates torp C2's overwrite guard (ECOSYSTEM-FIX-PLAN.md T3): a
transient read of the existing locked-predictions file (any error other
than a confirmed-absent 404) aborts rather than being treated as "no
existing file" -- that collapse is exactly what let one week's data
silently replace a full season of locked history. A fresh, non-accumulating
upload is only permitted after independently confirming via
\code{\link[=vb_confirm_absent]{vb_confirm_absent()}} that the release asset really is absent. An
accumulating merge is floor-guarded against a >10\% shrink via
\code{\link[=vb_guard_accumulate]{vb_guard_accumulate()}}.
}
\keyword{internal}
