% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/versebus.R
\name{.vb_retry}
\alias{.vb_retry}
\title{Retry a fallible operation with short exponential backoff}
\usage{
.vb_retry(fn, times = 3L, delays = c(2, 5), should_retry = function(e) TRUE)
}
\arguments{
\item{fn}{zero-arg function to attempt}

\item{times}{maximum attempts (default 3: one try + 2 retries)}

\item{delays}{seconds to sleep before each retry (default 2, then 5)}

\item{should_retry}{function(error) -> logical; FALSE re-raises immediately}
}
\description{
GitHub's release-asset CDN throws sporadic 5xx errors (torpdata#66/#68)
that usually clear on a second or third attempt with no code change --
this mirrors the backoff style \code{save_to_release()} already uses for its
upload-side 404/422 retry. Retries up to \code{times} attempts total, sleeping
\code{delays[i]} seconds before each retry (recycled if \code{times - 1} exceeds
\code{length(delays)}). \code{should_retry} lets the caller exclude confirmed-absent
failures (e.g. a real 404) -- those never resolve by waiting.
}
\keyword{internal}
