# Select AFL Match Prediction Model Variables

This function is intended for internal use and may be unexported in a
future release.

## Usage

``` r
select_afl_model_vars(df)
```

## Arguments

- df:

  A dataframe containing AFL team model data

## Value

A dataframe with selected variables for AFL modeling
