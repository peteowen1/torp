# Add quarter variables (Pass 3)

Adds quarter-level variables grouped by match_id and period. Modifies
the data.table by reference.

## Usage

``` r
add_quarter_vars_dt(dt)
```

## Arguments

- dt:

  A data.table to modify

## Value

Invisible NULL (modifies dt by reference)
