% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/xg.R
\name{get_xg}
\alias{get_xg}
\title{Get Live xG for a Match or Round}
\usage{
get_xg(
  match = NULL,
  season = get_afl_season(),
  round = NULL,
  quarter = 1:4,
  detail = FALSE
)
}
\arguments{
\item{match}{Input: either a match ID string (e.g. \code{"CD_M20260140201"})
or a pre-scraped chains data.frame from \code{\link[=get_match_chains]{get_match_chains()}}.
If \code{NULL} (default), uses \code{season} and \code{round} to fetch chains.}

\item{season}{Numeric season year (default: current season via
\code{\link[=get_afl_season]{get_afl_season()}}). Only used when \code{match} is \code{NULL}.}

\item{round}{Numeric round number. Only used when \code{match} is \code{NULL}.}

\item{quarter}{Numeric vector of quarters to include (default: \code{1:4}).}

\item{detail}{Logical. If \code{TRUE}, returns the full play-by-play data
(filtered to shots) instead of match-level summaries. Useful for
debugging xG values. Default is \code{FALSE}.}
}
\value{
When \code{detail = FALSE} (default), a tibble with one row per match.
When \code{detail = TRUE}, the full shot-level PBP with columns like
\code{xscore}, \code{goal_prob}, \code{behind_prob}, \code{points_shot}, \code{shot_row}.
}
\description{
Scrapes chain data from the AFL API, runs the shot model pipeline, and
computes match-level xG summaries. Useful for matches that just finished
before the daily torpdata pipeline has run.
}
\examples{
\dontrun{
# From match ID
get_xg("CD_M20260140201")

# From season and round (all matches in the round)
get_xg(round = 2)
get_xg(season = 2025, round = 14)

# Debug: return shot-level data
get_xg(round = 2, detail = TRUE)

# From pre-scraped chains
chains <- get_match_chains(2026, 2)
get_xg(chains)
}
}
