% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opponent_adjustment.R
\name{adjust_stat_ratings_for_opponents}
\alias{adjust_stat_ratings_for_opponents}
\title{Adjust player stat ratings for opponent quality}
\usage{
adjust_stat_ratings_for_opponents(
  stat_ratings,
  stat_rating_data,
  ref_date,
  lambda_decay = OPP_ADJ_LAMBDA_DECAY,
  cap = OPP_ADJ_FACTOR_CAP,
  stat_defs = NULL
)
}
\arguments{
\item{stat_ratings}{data.table of player stat ratings from
\code{\link[=estimate_player_stat_ratings]{estimate_player_stat_ratings()}}, with \verb{\{stat\}_rating} columns.}

\item{stat_rating_data}{data.table from \code{.prepare_stat_rating_data()}.
Must include \code{team}, \code{opponent}, and \code{match_id} columns.}

\item{ref_date}{Date. Only matches before this date contribute to
opponent defensive profiles.}

\item{lambda_decay}{Numeric. Decay rate per day for opponent profile
recency weighting. Default uses \code{OPP_ADJ_LAMBDA_DECAY}.}

\item{cap}{Numeric vector of length 2: floor and ceiling for
adjustment factors. Default uses \code{OPP_ADJ_FACTOR_CAP}.}

\item{stat_defs}{Output of \code{\link[=stat_rating_definitions]{stat_rating_definitions()}}. If NULL, uses default.}
}
\value{
The input \code{stat_ratings} with additional \verb{\{stat\}_adj_rating}
columns for each rate stat. Original columns are unchanged.
}
\description{
For each rate stat, computes an opponent adjustment factor based on
how much of that stat each opponent concedes relative to league average.
Players who faced tougher schedules get their ratings boosted.
}
