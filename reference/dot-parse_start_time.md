# Parse start_time strings to POSIXct

Handles the local-time formatted strings stored in predictions data.

## Usage

``` r
.parse_start_time(x)
```

## Arguments

- x:

  Character vector of start time strings

## Value

POSIXct vector
