% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_train.R
\name{.predict_all_rows}
\alias{.predict_all_rows}
\title{Score every row of a frame, refusing to return a misaligned vector}
\usage{
.predict_all_rows(model, df, feature_cols)
}
\arguments{
\item{model}{A trained \code{xgb.Booster}.}

\item{df}{Frame to score. Every row gets a prediction.}

\item{feature_cols}{Character vector of feature columns.}
}
\value{
Numeric vector, guaranteed \code{length() == nrow(df)}.
}
\description{
\code{model.matrix()}'s default \code{na.action} is \code{na.omit}, so ANY row carrying an
NA in a feature column is silently DROPPED and the caller gets back fewer
predictions than the frame has rows. \code{.train_match_xgb()} assigns the result
straight onto the frame (\code{team_mdl_df$xgb_pred_... <- ...}), and that fails
two different ways depending on arithmetic:
}
\details{
\itemize{
\item lengths do not divide — \code{"replacement has N rows, data has M"}, raised far
from the cause and naming neither the NAs nor the matches involved;
\item lengths DO divide — R \strong{recycles silently}, attaching every prediction
after the first gap to the wrong match, with nothing logged.
}

The second is why this is a guard and not a tidy-up.

Not hypothetical: placeholder finals fixtures (teams TBD) carry NA rating
features every year from the moment the AFL publishes the finals schedule,
which is exactly when predictions matter most. Bit twice on 2026-07-29 in two
different places.

\strong{The obvious one-line fix does not work.} Passing \code{na.action = na.pass} to
\code{model.matrix()} still drops the rows — measured, 5-row frame in, 3 rows out.
The \code{na.action} has to reach \code{model.frame()}, hence the detour below.
XGBoost then routes NA down the default branch it learned at training time,
so the vector comes back full length and finite.

Training is unaffected — \code{train_step()} is fed completed matches only.
}
\keyword{internal}
