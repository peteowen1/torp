% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/player_attribution.R
\name{calculate_player_attribution}
\alias{calculate_player_attribution}
\title{Calculate Player Attribution for a Match}
\usage{
calculate_player_attribution(
  match_features,
  predict_fn,
  player_rating_cols,
  baseline_value = 0
)
}
\arguments{
\item{match_features}{Data frame. One row of match features including
player-level rating aggregations (e.g., from \code{aggregate_lineup_ratings()}).}

\item{predict_fn}{Function. Takes a feature data frame and returns
a prediction (e.g., win probability or expected goals).}

\item{player_rating_cols}{Character vector. Column names containing
player-aggregated ratings to ablate.}

\item{baseline_value}{Numeric. Value to use when ablating (default 0,
representing league average for centered ratings).}
}
\value{
Data frame with columns:
\itemize{
\item \code{feature}: Rating column name
\item \code{full_pred}: Prediction with all features
\item \code{ablated_pred}: Prediction with this feature zeroed
\item \code{contribution}: \code{full_pred - ablated_pred} (positive = helps team)
\item \code{pct_contribution}: Contribution as percentage of total
}
}
\description{
Measures how much each player's ratings contribute to the match
prediction by comparing full prediction vs prediction with that
player's ratings set to league average (zero-ablation).
}
\examples{
\dontrun{
features <- data.frame(
  home_panna_sum = 5.2, away_panna_sum = 3.1,
  home_offense_mean = 0.8, away_offense_mean = 0.5,
  elo_diff = 50
)
predict_fn <- function(x) 1 / (1 + exp(-x$elo_diff / 100))
rating_cols <- c("home_panna_sum", "away_panna_sum",
                 "home_offense_mean", "away_offense_mean")
attr <- calculate_player_attribution(features, predict_fn, rating_cols)
}
}
