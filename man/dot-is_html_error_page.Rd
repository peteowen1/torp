% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scraper.R
\name{.is_html_error_page}
\alias{.is_html_error_page}
\title{Detect an HTML error/maintenance page returned where structured data was expected}
\usage{
.is_html_error_page(body)
}
\arguments{
\item{body}{Character. Response body already read as text.}
}
\value{
Logical
}
\description{
AFL's API gateways occasionally return an HTML error page (transient
upstream 5xx/maintenance) with a 200 status and/or a JSON content-type
header, so \code{httr::stop_for_status()} doesn't catch it and the raw markup
gets thrown straight into \code{jsonlite::fromJSON()}, whose lexer error
embeds a chunk of the document verbatim (torpdata#71 -- the raw error was
literally \verb{<!DOCTYPE html...}). Sniff the body itself instead: a real API
response never starts with a doctype/html tag.
}
\keyword{internal}
