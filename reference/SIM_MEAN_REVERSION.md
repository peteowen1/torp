# Mean reversion rate per round Fraction of the gap between a team's rating and the league mean that is closed each round. 0.01 = ~21% total reversion over 24 rounds.

Mean reversion rate per round Fraction of the gap between a team's
rating and the league mean that is closed each round. 0.01 = ~21% total
reversion over 24 rounds.

## Usage

``` r
SIM_MEAN_REVERSION
```

## Format

An object of class `numeric` of length 1.
