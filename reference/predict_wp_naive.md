# Naive Baseline Model for Win Probability

Simple baseline that always predicts 50% win probability

## Usage

``` r
predict_wp_naive(data)
```

## Arguments

- data:

  Input dataframe (not used, for consistency)

## Value

Vector of 0.5 predictions
