# Offset added to Haversine distance (metres) before log-transform Prevents log(0) for home games and dampens the curve for short trips

Offset added to Haversine distance (metres) before log-transform
Prevents log(0) for home games and dampens the curve for short trips

## Usage

``` r
MATCH_LOG_DIST_OFFSET
```

## Format

An object of class `numeric` of length 1.
