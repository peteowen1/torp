# Individual field position codes for match models (18 on-field positions)

Individual field position codes for match models (18 on-field positions)

## Usage

``` r
MATCH_INDIVIDUAL_POS
```

## Format

An object of class `character` of length 18.
