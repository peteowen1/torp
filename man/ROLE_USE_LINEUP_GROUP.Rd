% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constants_ratings.R
\docType{data}
\name{ROLE_USE_LINEUP_GROUP}
\alias{ROLE_USE_LINEUP_GROUP}
\title{Group the EPV role adjustment on \code{lineup_group} instead of raw \code{lineup_position}}
\format{
An object of class \code{logical} of length 1.
}
\usage{
ROLE_USE_LINEUP_GROUP
}
\description{
\code{.position_adjust()} groups by all 21 raw slots. Five of those are arbitrary
left/right mirrors (BPL/BPR, HBFL/HBFR, WL/WR, HFFL/HFFR, FPL/FPR) -- left
wing and right wing are the same job. Grouping them separately forces each
side to average zero INDEPENDENTLY, erasing any real difference between them
and halving the cell size for nothing. See \code{LINEUP_GROUP_MAP}.
}
\details{
Note this is NOT the granularity question §7.15 closed. That compared a 6-way
vs 9-way collapse at the RATING layer and found nothing (Δ +0.013, P 0.417).
The value layer collapses nothing today, so it has never been measured.

Applies to BOTH value layers -- EPV's \code{.position_adjust()} grouping and PSV's
role-centring pass -- because they must stay built the same way. An earlier
draft of this work assumed PSV had no weekly stage at all and tried to add
one; it already has one (\code{psr.R}, the \code{pos_col} block), keyed on the same raw
\code{lineup_position}. The two value layers are already symmetric; the open
question is only whether the mirrors should be merged.
}
\keyword{internal}
