% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_stoppage_cells}
\alias{.np_stoppage_cells}
\title{Which cell a stoppage falls in: type x 20m band x distance from the corridor}
\usage{
.np_stoppage_cells(d)
}
\arguments{
\item{d}{Table with \code{description}, \code{home}, \code{x}, and optionally \code{chain_x_home}
and \code{chain_aby}. Modified by reference.}
}
\value{
Count of rows that had a true chains location.
}
\description{
\code{.np_stoppage_baseline()} estimates the cell means and \code{.np_build_ledger()}
charges each stoppage against them. They must key on exactly the same thing
or a stoppage is priced from one cell and charged to another, so both call
this one function rather than each writing the expression out.
}
\details{
Location comes from chains where it exists (\code{chain_x_home}, \code{chain_aby}) and
falls back to PBP's own x, which is the location of the stoppage's OUTCOME
rather than of the stoppage.
}
\keyword{internal}
