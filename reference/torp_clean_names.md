# Clean column names to snake_case

Lightweight replacement for janitor::clean_names(). Converts column
names to snake_case by handling CamelCase, dots, spaces, and special
characters.

## Usage

``` r
torp_clean_names(df)
```

## Arguments

- df:

  A data frame.

## Value

The data frame with cleaned column names.
