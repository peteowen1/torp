% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wpa_net_ledger.R
\name{build_wpa_ledger}
\alias{build_wpa_ledger}
\title{Build the WPA Net Points ledger}
\usage{
build_wpa_ledger(
  pbp_data,
  player_stats,
  pre_match,
  results = NULL,
  scale = WPA_LEDGER_SCALE
)
}
\arguments{
\item{pbp_data}{Play-by-play carrying \code{wpa} plus everything
\code{build_net_points()} needs.}

\item{player_stats}{Box-score stats for the same matches (time on ground
spreads the team pools).}

\item{pre_match}{A data frame of \code{match_id} and \code{home_win_prob}, and
optionally \code{source} (recorded as \code{p0_source}, e.g. "locked" or
"retrodiction"). Matches with no row are left out of the ledger, with a
warning, and listed in the \code{skipped} attribute.}

\item{results}{Official results, as for \code{.np_match_results()}; \code{NULL}
loads them.}

\item{scale}{Units the engine runs in; see \code{WPA_LEDGER_SCALE}.}
}
\value{
A data.table, one row per player-match, of \code{player_id},
\code{match_id}, \code{wpa_net}, and the three parts
\code{wpa_own} + \code{wpa_won} + \code{wpa_team} = \code{wpa_net}. All in win probability
(fractions), each in the player's own team frame. Attribute \code{targets}
holds each match's \code{p0}, its source, the result and the home target;
attribute \code{skipped} the match_ids left out for want of a forecast.
}
\description{
Runs per-play win probability change through \code{build_net_points()} so each
team's players sum to that team's result minus its pre-match win chance.
Run it with \code{pre_match = .wpa_neutral_pre_match(...)} for the team-neutral
version, where every match starts at the same home win chance.
}
\details{
The engine runs in its plain configuration (\code{credit = "flat"},
\code{stoppages = "exclude"}): the difficulty terms and stoppage baselines the
EPV engine uses are fitted in points and do not transfer to win
probability. Its log messages say "points"; here they mean percentage
points of win probability (see \code{WPA_LEDGER_SCALE}).
}
