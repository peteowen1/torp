% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{load_ep_wp_charts}
\alias{load_ep_wp_charts}
\title{Load EP/WP Chart Data}
\usage{
load_ep_wp_charts(
  seasons = get_afl_season(),
  rounds = TRUE,
  use_disk_cache = FALSE,
  columns = NULL
)
}
\arguments{
\item{seasons}{A numeric vector of 4-digit years associated with given AFL
seasons — defaults to latest season. If set to \code{TRUE}, returns all
available data since 2021.}

\item{rounds}{A numeric vector associated with given AFL round — defaults to
all rounds. If set to \code{TRUE}, returns all available rounds in the given
season range.}

\item{use_disk_cache}{Logical. If \code{TRUE}, uses persistent disk cache for
faster repeated loads. Default is \code{FALSE}.}

\item{columns}{Optional character vector of column names to read. If NULL (default), reads all columns.}
}
\value{
A data frame containing EP/WP chart data with columns including
\code{match_id}, \code{season}, \code{round_number}, \code{period}, \code{total_seconds},
\code{home_team_name}, \code{away_team_name}, \code{team}, \code{exp_pts},
\code{delta_epv}, \code{wp}, \code{wpa}, \code{description}, \code{player_name}, \code{play_type},
\code{shot_row}, and \code{points_shot}.
}
\description{
Loads a lightweight subset of play-by-play data optimised for
charting Expected Points (EP) and Win Probability (WP) over a match.
Contains every play but only ~25 columns instead of the full 150+
available from \code{\link[=load_pbp]{load_pbp()}}.
}
\examples{
\dontrun{
try({ # prevents cran errors
  load_ep_wp_charts(2024)
})
}
}
\seealso{
\code{\link[=load_pbp]{load_pbp()}}, \code{\link[=load_xg]{load_xg()}}
}
