% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{np_difficulty_terms_for_season}
\alias{np_difficulty_terms_for_season}
\title{Difficulty terms for one season, fitted on another (the pipeline's entry point)}
\usage{
np_difficulty_terms_for_season(
  season,
  pbp_data = NULL,
  chains = NULL,
  train_season = NULL,
  available = 2021:get_afl_season()
)
}
\arguments{
\item{season}{Season to score.}

\item{pbp_data, chains}{The target season's data (loaded when \code{NULL}).}

\item{train_season}{Season to fit on. Defaults to \code{season - 1}, or
\code{season + 1} when \code{season} is the earliest in \code{available}.}

\item{available}{Seasons chains exist for.}
}
\value{
\code{.np_difficulty_terms()} output for \code{season}.
}
\description{
Loads only the columns the models read for the training season, fits, scores
the target season and frees the training data. The default training season
is the one before; the first season we hold chains for (2021) is fitted on
the one after, a reverse fit that touches only that season (D14).
}
