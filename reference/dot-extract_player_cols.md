# Extract column values from a single-row data.table, defaulting to NA

Extract column values from a single-row data.table, defaulting to NA

## Usage

``` r
.extract_player_cols(player_row, col_names)
```
