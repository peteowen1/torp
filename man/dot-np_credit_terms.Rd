% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/epv_net_points.R
\name{.np_credit_terms}
\alias{.np_credit_terms}
\title{Decide, per ledger row, who is paid what}
\usage{
.np_credit_terms(led, credit, alpha, phi, beta = 0, omega = 0, terms = NULL)
}
\arguments{
\item{led}{Ledger from \code{.np_build_ledger()}.}

\item{credit}{\code{"flat"} or \code{"difficulty"}.}

\item{alpha}{Flat receiver share.}

\item{phi}{Flat defensive share.}

\item{beta}{Disposer's share of a lost surprise (difficulty).}

\item{omega}{Offence team-pool share (difficulty).}

\item{terms}{Output of \code{.np_difficulty_terms()}, or \code{NULL} under flat.}
}
\value{
\code{led} with \code{kind}, \code{scored}, \code{contested} and the six parts \code{own_hm},
\code{recv_hm}, \code{win_hm} (a same-team contest winner), \code{team_hm}, \code{cede_hm}
(ground-ball value ceded to the opposition) and \code{cede_c_hm} (contest
value ceded to the opposition, routed to the contest winner by
\code{NP_CONTEST_WINNER_SHARE}).
}
\description{
Every row's \code{hm} is split into four non-overlapping parts that sum back to
it exactly:
}
\details{
\describe{
\item{\code{own_hm}}{what the actor keeps}
\item{\code{recv_hm}}{paid to the receiver of a retained disposal}
\item{\code{team_hm}}{paid to the actor's team pool (offence, D9)}
\item{\code{cede_hm}}{paid to the OPPOSITION for a turnover, routed by
\code{.np_defensive_pool()} (ball-winner share, then the spread)}
}

Under \code{credit = "flat"} the split is the pre-v4 rule: a retained disposal
gives \code{alpha} to the receiver, a turnover cedes \code{phi}, nothing goes to an
offence pool. Under \code{credit = "difficulty"} a scored disposal follows the
identity in \code{.np_difficulty_terms()}:

\itemize{
\item retained: actor \code{(1 - omega) * (decision + p * surprise)}, receiver
\code{(1 - omega) * (1 - p) * surprise}, team pool \code{omega * hm} (D6, D9);
\item turnover: actor \code{decision + beta * surprise}, opposition
\code{(1 - beta) * surprise} (D7);
\item terminal (a score, a restart): actor \code{(1 - omega) * hm}, team pool
\code{omega * hm}.
}

A disposal the difficulty model could not score (no resolvable outcome row,
4.8\% in 2026) falls back to the flat rule; the count is logged, because a
silent fallback here would be indistinguishable from the rule working.
}
\keyword{internal}
