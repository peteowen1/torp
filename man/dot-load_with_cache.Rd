% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_data.R
\name{.load_with_cache}
\alias{.load_with_cache}
\title{Load data from AFL API with caching}
\usage{
.load_with_cache(
  cache_prefix,
  seasons,
  fetch_fn,
  use_cache = TRUE,
  cache_ttl = 3600,
  verbose = FALSE,
  columns = NULL,
  fetch_all_fn = NULL,
  use_disk_cache = FALSE,
  refresh = FALSE
)
}
\arguments{
\item{cache_prefix}{Character. Cache key prefix (e.g. "fixtures", "results").}

\item{seasons}{Numeric vector of seasons.}

\item{fetch_fn}{Function that takes a single season year and returns a
data.frame/tibble.}

\item{use_cache}{Logical. Whether to use in-memory caching.}

\item{cache_ttl}{Numeric. Cache time-to-live in seconds.}

\item{verbose}{Logical. Print cache hit/miss info.}

\item{columns}{Optional character vector of column names to select.}

\item{fetch_all_fn}{Optional function that takes a vector of seasons and
returns all data in one batch. When provided and \code{length(seasons) > 1},
used instead of per-season \code{lapply(seasons, fetch_fn)} for efficiency
(e.g. one big \code{curl::multi_download()} instead of N sequential batches).}
}
\value{
A tibble.
}
\description{
Generalised helper that wraps any \verb{get_afl_*()} function with per-season
fetching, in-memory caching, and column selection. Used by \code{load_fixtures()},
\code{load_results()}, \code{load_teams()}, \code{load_player_stats()}, and
\code{load_player_details()}.
}
\keyword{internal}
