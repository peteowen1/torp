% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/np_breakdown.R
\name{.np_breakdown}
\alias{.np_breakdown}
\title{Split each player-match's Net Points by play type}
\usage{
.np_breakdown(np, pbp_data, tol = 1e-09)
}
\arguments{
\item{np}{Output of \code{.np_team_margin()}, carrying both attributes.}

\item{pbp_data}{Play-by-play for the same matches (for \code{description}).}

\item{tol}{Largest allowed gap between a player-match's categories and its
\code{net_points}. The default is rounding level: anything bigger means a
payment was dropped, double-counted or mislabelled.}
}
\value{
data.table with \code{match_id}, \code{player_id}, \code{category}, \code{value}
(points, positive is good for the player).
}
\description{
Every player-match's \code{net_points} is three parts that add up exactly: the
payments made to him by name, his share of the team pool, and his share of
the anchor to the real margin (\code{attr(np, "np_team_margin_parts")}). This
splits the named part by play type and role, using the payment rows at
their final value (\code{attr(np, "np_team_margin_payments")}), so the rows for a
player-match sum to its \code{net_points} with nothing left over.
}
\keyword{internal}
