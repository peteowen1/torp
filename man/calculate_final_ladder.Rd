% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ladder.R
\name{calculate_final_ladder}
\alias{calculate_final_ladder}
\title{Calculate expected final ladder from actual results + predictions}
\usage{
calculate_final_ladder(
  season = get_afl_season(),
  fixtures = NULL,
  predictions = NULL
)
}
\arguments{
\item{season}{Numeric season year (e.g. 2026).}

\item{fixtures}{Optional fixture data. If NULL, loads via \code{\link[=load_fixtures]{load_fixtures()}}.}

\item{predictions}{Optional predictions data. If NULL, loads via
\code{\link[=load_predictions]{load_predictions()}}.}
}
\value{
A data.table with one row per team, sorted by expected ladder
position. Columns: \code{team}, \code{played}, \code{wins}, \code{draws}, \code{losses},
\code{expected_wins}, \code{expected_losses}, \code{points_for}, \code{points_against},
\code{percentage}, \code{ladder_points}, \code{rank}.
}
\description{
Combines played game results with model predictions for remaining games
to produce a single deterministic expected end-of-season ladder. Uses
fractional wins (pred_win) for unplayed games, giving a smooth expected
ladder that properly reflects match uncertainty.
}
