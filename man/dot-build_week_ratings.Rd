% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/match_data_prep.R
\name{.build_week_ratings}
\alias{.build_week_ratings}
\title{Build per-week roster ratings for one target week}
\usage{
.build_week_ratings(tr_data, w, target_weeks)
}
\arguments{
\item{tr_data}{Roster ratings with \verb{epr*}, \code{psr}, \code{pred_tog}, \code{injury} and
\code{return_round} columns.}

\item{w}{Target week (round number) to build ratings for.}

\item{target_weeks}{All weeks being predicted; the horizon discount is
measured from \code{min(target_weeks)}.}
}
\value{
One row per (team_name, season), with \code{round} set to \code{w}.
}
\description{
Roster-based counterpart to \code{\link[=.build_team_ratings_df]{.build_team_ratings_df()}}, which is
lineup-based. Only excludes players still injured for that week
(\code{return_round > w}); players returning by the target week are included.
The discount scales with prediction horizon (0.99 for the nearest week,
dropping 0.01 per week, floored at \code{INJURY_DISCOUNT_FLOOR}).
}
\details{
Single copy of an aggregation that until 2026-08-11 was pasted verbatim into
both \code{run_predictions_pipeline()} (match_model.R) and
\code{build_matchup_table()} (matchup_table.R) -- the two differed only in a line
wrap, so a new EPR channel or a changed TOG scaling had to be edited twice or
the blog's matchup table would silently disagree with the published
predictions.
}
\keyword{internal}
