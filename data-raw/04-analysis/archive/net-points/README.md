# Archived net points analyses

One-off experiments from building Net Points (2026-09-04 → 09-23), moved here on
2026-09-23 so `data-raw/04-analysis/` shows only what is still run. Nothing here
is sourced by the package, a workflow or a test. Git history has every version.

Scripts whose result is cited somewhere (DECISIONS.md, NEXT-STEPS.md,
`docs/reference/PLAY-TYPES.md`, `docs/plans/EPV-V4-CREDIT-RULES.md`, or the
package roxygen) are kept so the citation still resolves by file name; the rest
were referenced nowhere. The audit that sorted them is in the torpverse session
notes for 2026-09-23 (vault/plans/NET-LEDGER-PARITY.md, work plan step 5).

Still live in `data-raw/04-analysis/`: `build_np_categories_artifact.R`,
`np_first_goal_walkthrough.R`, `np_season_build.R`, `np_anchor_anatomy.R`,
`np_book_conceding_ab.R`, `np_pool_dacts_ab.R`, `np_v14_check.R`,
`np_validate.R`, `np_row_audit.R`, `np_yoy_shares.R`,
`build_net_points_scenarios.R`, `build_net_points_explainer.R`,
`run_epr_gate_v3v4.R`, and the page templates in `templates/`.
