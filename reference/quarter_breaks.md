# Quarter break positions for game flow charts

Returns a named numeric vector of quarter boundary seconds for x-axis
annotation.

## Usage

``` r
quarter_breaks()
```

## Value

Named numeric vector with quarter boundaries.
