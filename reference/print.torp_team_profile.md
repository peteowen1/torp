# Print a team profile

Print a team profile

## Usage

``` r
# S3 method for class 'torp_team_profile'
print(x, ...)
```

## Arguments

- x:

  A `torp_team_profile` object.

- ...:

  Additional arguments (ignored).

## Value

Invisibly returns `x`.
