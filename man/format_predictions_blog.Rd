% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_blog.R
\name{format_predictions_blog}
\alias{format_predictions_blog}
\title{Format match predictions for the blog / R2 parquet}
\usage{
format_predictions_blog(preds, xg = NULL)
}
\arguments{
\item{preds}{Data frame with the core blog columns: \code{season}, \code{round},
\code{home_team}, \code{away_team}, \code{home_epr}, \code{away_epr}, \code{pred_margin},
\code{home_win_prob}, \code{pred_total}, \code{actual_margin}, \code{start_time}, \code{venue}.}

\item{xg}{Optional lookup with columns \code{season}, \code{round}, \code{home_team},
\code{away_team}, \code{xscore_home}, \code{xscore_away}. Use \code{\link[=xg_to_blog_lookup]{xg_to_blog_lookup()}} to
build this from \code{\link[=load_xg]{load_xg()}} or \code{\link[=get_xg]{get_xg()}} output. If \code{NULL}, xscore
columns are written as \code{NA_real_} so the blog reader never hits
\code{undefined}.}
}
\value{
Tibble with exactly the columns in \code{PREDICTIONS_BLOG_COLS}, in
canonical order. Fails loudly if input is missing any required column.
}
\description{
Single source of truth for the \code{predictions.parquet} schema consumed by the
inthegame-blog \code{afl/matches.qmd} page. Called by both the CI pipeline
(\code{torpdata/scripts/build_blog_data.R}) and the local convenience pusher
(\code{torp/data-raw/02-models/push_predictions_to_r2.R}) so schema drift between
the two producers is impossible.
}
