% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/injuries.R
\name{tbc_played_rate}
\alias{tbc_played_rate}
\title{TBC Listing Played-Rate}
\usage{
tbc_played_rate(season = NULL, round = NULL)
}
\arguments{
\item{season}{Season year. Defaults to current via \code{\link[=get_afl_season]{get_afl_season()}}.}

\item{round}{Optional round number (or vector) to filter to.}
}
\value{
A tibble with one row per TBC listing: \code{round}, \code{player}, \code{team},
\code{injury}, \code{scraped_at}, \code{played} (logical). Attached \code{summary} attribute
is a list with elements:
\itemize{
\item \code{n_listings} — total listing-round rows
\item \code{overall_played_pct} — fraction of listings where the player played
\item \code{per_round} — tibble of \code{round}, \code{n}, \code{played}, \code{played_pct}
}
}
\description{
Per-round analogue of \code{\link[=test_played_rate]{test_played_rate()}} for players listed as "TBC"
(estimated return unknown). For each completed round, finds players whose
latest pre-kickoff weekly listing says "TBC" and checks whether they were
named in the selected 22. The overall played fraction here directly
calibrates the \code{current_round + 3} fallback used by \code{\link[=parse_return_round]{parse_return_round()}}
for TBC entries.
}
\details{
Also returns a \code{summary} attribute with the overall and per-round played
rate -- useful for dashboards and for feeding back into simulation
defaults.
}
